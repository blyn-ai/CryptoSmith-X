{
  "ConnectionStrings": {
    "Database": "Host=localhost;Port=5432;Database=marketdata;Username=marketdata;Password=marketdata",
    "TradingBotDatabase": "Host=localhost;Port=5433;Database=tradingbot;Username=tradingbot;Password=tradingbot"
  },
  "TradingBot": {
    "BaseUrl": "https://blynai.meetluko.eu",

    "// Instances": "username -> bot_instance_id. An account absent from this map owns no bot and the screen says so.",
    "Instances": {
      "denis": "futures-live",
      "lukas": "futures-lukas-live"
    },

    "// Baselines": "A MIRROR of each worker's own appsettings — what it runs on with no override rows. Shown, and labelled as such, when a key has no row. It can drift from the bot's real deployment, which is why the screen never presents it as something the owner set.",
    "Baselines": {
      "futures-live": {
        "PositionMarginUsd": 10,
        "Leverage": 1,
        "MaxOpenPositions": 3,
        "MaxOpenPositionsPerGroup": 1
      },
      "futures-lukas-live": {
        "PositionMarginUsd": 15,
        "Leverage": 10,
        "MaxOpenPositions": 3,
        "MaxOpenPositionsPerGroup": 1
      }
    }
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*"
}
