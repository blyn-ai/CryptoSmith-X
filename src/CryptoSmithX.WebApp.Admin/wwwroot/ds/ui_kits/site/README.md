# Site UI kit

The CryptoSmith X public "about" page — static dark marketing surface. Hero with the full live-text wordmark lockup, telemetry plate (the only gold-bordered, glowing element), venue strip, 4-step method, BlynAI byline, compliance footer.

Single file: `index.html`. Pure tokens + `styles.css`; no React needed.
