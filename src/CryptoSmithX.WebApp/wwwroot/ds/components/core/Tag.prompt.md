Mono-caps status chip. Tones: `gold` (running/live), `violet` (AI/inference), `neutral` (paused), `up`/`down` (market).

```jsx
<Tag tone="gold">Running</Tag>
<Tag tone="violet">AI watchlist</Tag>
```
