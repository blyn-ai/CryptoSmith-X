The STATE column, as a symbol rather than a word.

```jsx
<StateDot state="observed" title="Observed — snapshot present, collection on" />
<StateDot state="none" title="No data — nothing has been observed" />
```

Public surfaces show two states only. `not collected` is an admin decision, not a fact about
the market, and does not belong on a public page.
