The band row above the column eyebrows, one stretch per call.

```jsx
<CallBands template={COLS} venueSpan={3} tickerSpan={10} oiSpan={1} depthSpan={3} />
```

Pass the same `template` string every row uses; the bands align by grid, not by pixels.
