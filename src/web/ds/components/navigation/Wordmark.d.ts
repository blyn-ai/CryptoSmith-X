/** The name lockup as live text — Space Grotesk 600, X in violet; optional mono descriptor line. */
export interface WordmarkProps {
  /** Title font size in px (default 16) */
  size?: number;
  /** Show "PERPS & CRYPTO TRADE BOT" descriptor at 0.4× */
  descriptor?: boolean;
}
