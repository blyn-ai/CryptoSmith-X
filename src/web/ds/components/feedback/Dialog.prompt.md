Confirmation modal.

```jsx
<Dialog open title="Stop strategy?" danger confirmLabel="Stop" onConfirm={stop} onCancel={close}>
  Open positions stay open; the bot just stops managing them.
</Dialog>
```
