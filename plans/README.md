# plans

Задания, которые ещё не выполнены. Ничего больше.

Файл живёт здесь ровно до того момента, пока написанное в нём не окажется в коде.
Как только окажется — файл удаляется, потому что дальше он врёт: описывает как
намерение то, что уже стало фактом, и читатель не может отличить одно от другого.
История в гите никуда не девается, если понадобится посмотреть, чего хотели.

Что здесь **не** лежит: описание того, как система устроена (это `docs/` и
`architecture.mermaid`), принятые решения (`docs/decisions.md`), эксплуатация
(`docs/recovery-playbook.md`).
