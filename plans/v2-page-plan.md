# План: довести /studio/v2 до макета дизайнера на реальных данных

Состояние на начало: полосы 1–3 есть, 4 и 5 нет, часть полей таблицы пустует.
Правило одно: **всё из реальных данных; чего площадка не даёт — прочерк, а не выдумка.**

## 1. Достроить снимок в запросе — «дешёвые» дыры таблицы

`StudioStore.PairVenuesSql` не знает о колонках 0030, хотя в базе они заполнены.

- [ ] добавить в SELECT и в `PairVenueRow`: `venue_ts`, `last_trade_at`, `depth_ref`,
      `book_reach_bid`, `book_reach_ask`, `volume_24h_base`, `funding_rate_predicted`,
      `next_funding_at`
- [ ] `V2Cells.LastTrade` → настоящий `last_trade_at`, а не `received_at` (сейчас страница
      утверждает то, чего не измеряла)
- [ ] `V2Cells.BookReach` → настоящие bps; пустая сторона = 0, не прочерк
- [ ] `VenueClock` → `venue_ts`, прочерк где площадка не шлёт
- [ ] `LIQUIDITY`: крупное число — сумма сторон, мелкое — пара; сейчас крупное дублирует bid

## 2. Полоса 2 при выборе цены — стаканы, а не бары

`book_topn` наполняется; профиль читается формой, а не цифрами.

- [ ] `BookStore.ReadLatestAsync(conn, ids)` — последний кадр на инструмент:
      `bid_px[]`, `bid_qty[]`, `ask_px[]`, `ask_qty[]`, `observed_at`, `levels`
- [ ] отрисовать профиль: горизонтальные полосы по уровням, mid по центру, подпись
      «seen to ±N bps · 25 levels a side»
- [ ] где кадра нет — сказать словами «no level book from this venue», не пустая рамка

## 3. Полоса 4 — лента

- [ ] `TradeStore.ReadRecentAsync(conn, ids, limit: 18)` — время, площадка, сторона, цена,
      размер, `trade_type`
- [ ] ликвидации и termination выделяются, как в макете
- [ ] подпись: сколько сделок в секунду на этом активе и сколько показано

## 4. Полоса 5 — покрытие и пробелы

- [ ] `CoverageStore.ReadAsync` — доля удержанного окна по набору × площадке, в одну шкалу
      зелёного (одна метрика, одна ось площадок)
- [ ] `GapStore.ReadAsync` — открытые и закрытые пробелы с причиной и интервалом;
      `never` для того, чего площадка не публикует вовсе
- [ ] блок «everything else this asset has» — что храним и куда за этим идти

## 5. STRESS в таблице

- [ ] `liquidation_volume_history` за последние 5 минут на инструмент → объём, стороны,
      соотношение; поля в группе `stress`

## Чего не делать

- Не выдумывать значения там, где площадка молчит.
- Не заводить второй загрузчик: всё через `PairPageLoader`, одно чтение на страницу.
- Не трогать первую страницу.
- Версию `?v=` у CSS поднимать при каждой правке файла — Cloudflare кеширует.
