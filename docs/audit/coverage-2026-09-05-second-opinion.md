# Coverage audit — second opinion, independent of the 12:31 audit

- Commit: `7eece397820a868b371047f3fc98796fb8dd4fdf`
- Date: `2026-09-05`  ·  Branch: `main`
- Method: 16 section auditors run independently and in parallel, each reading the repository itself; every disagreement with the prior audit then handed to a separate checker **instructed to refute it**
- DB/host probes: **YES** — live probes against the deployed test site (`csx-prod`) and the VM, plus host filesystem
- Prior audit: `docs/audit/coverage-2026-09-05.md` (different model, probed 12:31 UTC)
- Subject is the **code**, not any one deployment

## A. Ticker snapshot (WP1, WP6)

WP1 is not started: no market_snapshot_10s, and none of the §4.1 columns (event_time, known_from, persisted_at, source, source_seq, collection_run_id, exchange_ts) exist anywhere in code or schema (grep over src/, schema ends at 0017).
The only persistent ticker store is legacy market_snapshot (0001_initial.sql:177-203), written once per minute by SnapshotCollector.cs:48-53,132 while the loop polls at 10 s — so 5 of 6 observations reach only the mutable market_snapshot_latest cache and are then overwritten.
market_snapshot uniformly fails S1, S2, S3, S4, S5, S8, S9; S6/S7 are N/A for a raw observation table. Every "stored" NO below carries that same set unless the note says otherwise.

| Item | Collected | Stored correctly | Evidence | Note |
|---|---|---|---|---|
| A1 every 10s observation persisted (~8640 rows/instrument/day) | PARTIAL | NO | SnapshotCollector.cs:48-53,132 gates the history insert to one write per minute; insert at SnapshotCollector.cs:144-162 → market_snapshot.received_at (0001_initial.sql:177-203). Probe A1: hyperliquid 1438.5, kraken-futures 1443.5, weex-futures 1422.4 rows/instrument/day vs required ~8640; probe: market_snapshot_10s ABSENT | PARTIAL: polled at 10 s (0014_collections.sql:184) but 5 of 6 observations die in market_snapshot_latest; fails S1,S2,S3,S4,S5,S8,S9. |
| A2 last price | PARTIAL | NO | KrakenFuturesMarketData.cs:109 (t.Last) / KrakenWsFeed.cs:180 / WeexFuturesMarketData.cs:142 / HyperliquidMarketData.cs:113,126 (midPx) → SnapshotCollector.cs:147,153 → market_snapshot.last_price (0001_initial.sql:181) | PARTIAL: Hyperliquid persists book midPx in a column named last_price with no tag saying it is a proxy; fails S1,S2,S3,S4,S5,S8,S9. |
| A3 bid | YES | NO | KrakenFuturesMarketData.cs:110 / KrakenWsFeed.cs:181 / WeexFuturesMarketData.cs:143 / HyperliquidBookMath.cs:31 via HyperliquidMarketData.cs:127 → SnapshotCollector.cs:147,153 → market_snapshot.bid_price (0001_initial.sql:182) | Full path on all three venues into a persistent partitioned table; table fails S1,S2,S3,S4,S5,S8,S9. |
| A4 ask | YES | NO | KrakenFuturesMarketData.cs:111 / KrakenWsFeed.cs:182 / WeexFuturesMarketData.cs:144 / HyperliquidBookMath.cs:31 via HyperliquidMarketData.cs:128 → SnapshotCollector.cs:147,153 → market_snapshot.ask_price (0001_initial.sql:183) | Full path on all three venues; table fails S1,S2,S3,S4,S5,S8,S9. |
| A5 bid size | YES | NO | KrakenFuturesMarketData.cs:112 / KrakenWsFeed.cs:183 / WeexFuturesMarketData.cs:145 (v3 bookTicker bidQty) / HyperliquidBookMath.cs:31 via HyperliquidMarketData.cs:129 → SnapshotCollector.cs:148,154 → market_snapshot.bid_size (0001_initial.sql:184) | Stored, but bid_size carries no unit comment on either table (0001_initial.sql:150-170 comments only latest, not sizes); fails S1,S2,S3,S4,S5,S8,S9. |
| A6 ask size | YES | NO | KrakenFuturesMarketData.cs:113 / KrakenWsFeed.cs:184 / WeexFuturesMarketData.cs:146 / HyperliquidBookMath.cs:31 via HyperliquidMarketData.cs:130 → SnapshotCollector.cs:148,154 → market_snapshot.ask_size (0001_initial.sql:185). Probe S3ZERO: ask_size=0 in 311 rows of 2,085,123 | Stored with no unit tag, and a zero-size ask is written as a measured 0; fails S1,S2,S3,S4,S5,S8,S9. |
| A7 mark | YES | NO | KrakenFuturesMarketData.cs:114 / KrakenWsFeed.cs:185 / WeexFuturesMarketData.cs:138,147 / HyperliquidMarketData.cs:131 (markPx) → SnapshotCollector.cs:148,154 → market_snapshot.mark_price (0001_initial.sql:186) | Full path on all three venues; table fails S1,S2,S3,S4,S5,S8,S9. |
| A8 index | YES | NO | KrakenFuturesMarketData.cs:115 / KrakenWsFeed.cs:186 (index) / WeexFuturesMarketData.cs:148 / HyperliquidMarketData.cs:132 (oraclePx) → SnapshotCollector.cs:148,154 → market_snapshot.index_price (0001_initial.sql:187) | Full path on all three venues; table fails S1,S2,S3,S4,S5,S8,S9. |
| A9 current funding rate | YES | NO | KrakenFuturesMarketData.cs:119 / KrakenWsFeed.cs:187 (relative_funding_rate) / WeexFuturesMarketData.cs:151 / HyperliquidMarketData.cs:135 → SnapshotCollector.cs:148,154 → market_snapshot.funding_rate (0001_initial.sql:188) | Kraken REST substitutes a literal 0 when mark<=0 (KrakenFuturesMarketData.cs:119) — an invented observation; fails S1,S2,S3,S4,S5,S8,S9. |
| A10 predicted funding | NO | NO | Provided by Kraken: Fixtures/kraken/tickers.json:20 fundingRatePrediction, Fixtures/kraken-ws/ticker.json:5,7. Not bound: KrakenDtos.cs:36-55, KrakenWsDtos.cs:10-28. No field on Ticker.cs:13-27 and no column on market_snapshot (0001_initial.sql:177-203) | Dropped at the DTO, no target column anywhere; fails S2,S5,S9. |
| A11 next funding time | NO | NO | Provided by Kraken WS: Fixtures/kraken-ws/ticker.json:8 next_funding_rate_time; omitted by KrakenWsDtos.cs:10-28 and Ticker.cs:13-27. 0001_initial.sql:21-23 deliberately drops the column saying the API computes it — no such computation exists (only exchange_instrument.funding_interval_hours is exposed, Endpoints.cs:110) | Neither stored nor derived; the spec it would derive from is overwritten in place (DiscoveryCollector.cs:115); fails S2,S5,S7,S9. |
| A12 open interest | YES | NO | KrakenFuturesMarketData.cs:121 / KrakenWsFeed.cs:189 / WeexFuturesMarketData.cs:133,153 / HyperliquidMarketData.cs:137 → SnapshotCollector.cs:149,155 → market_snapshot.open_interest, market_snapshot.open_interest_at (0001_initial.sql:191-192). Probe S3ZERO: open_interest=0 in 3601 rows | WEEX OI is up to 10 min old but honestly stamped (WeexOpenInterestFeed.cs:23,45-52); zeros are stored as measured; fails S1,S2,S3,S4,S5,S8,S9. |
| A13 24h volume (base) | NO | NO | Present in every payload — Fixtures/kraken/tickers.json:16 vol24h, Fixtures/weex/tickers.json:11 base_volume, Fixtures/hyperliquid/meta_and_ctxs.json:53 dayBaseVlm — and bound by none: KrakenDtos.cs:36-55, WeexDtos.cs:27-36, HyperliquidDtos.cs:23-31. No column on market_snapshot (0001_initial.sql:177-203) | Base volume is dropped on all three venues; only quote turnover survives; fails S2,S4,S5,S9. |
| A14 24h turnover (quote) | YES | NO | KrakenFuturesMarketData.cs:120 (volumeQuote) / KrakenWsFeed.cs:188 / WeexFuturesMarketData.cs:152 (volume_24h) / HyperliquidMarketData.cs:136 (dayNtlVlm) → SnapshotCollector.cs:148,154 → market_snapshot.turnover_24h (0001_initial.sql:189) | Full path; unit comment exists only on the mutable cache (0001_initial.sql:159-161), not on market_snapshot; fails S1,S2,S3,S4,S5,S8,S9. |
| A15 last-trade time | NO | NO | Kraken REST provides it: Fixtures/kraken/tickers.json:8 lastTime; not bound by KrakenDtos.cs:36-55. WEEX quote time Fixtures/weex/tickers.json:10 also unbound (WeexDtos.cs:27-36). No field on Ticker.cs:13-27, no column on market_snapshot (0001_initial.sql:177-203) | The one venue that publishes it has it discarded at the DTO; fails S2,S5,S9. |
| A16 per-exchange coverage — Kraken Futures | PARTIAL | NO | KrakenFuturesMarketData.cs:85-129 (REST) and KrakenWsFeed.cs:61-80,168-192 (WS) → SnapshotCollector.cs:32-175 → market_snapshot; hosted at ExchangeWorker.cs:256,264, enabled by 0014_collections.sql:184,218-227. Probe POLICY: kraken-futures enabled, snapshot collect@10; probe A1 1443.5 rows/day | A3-A9/A12/A14 land; A2 ok, A10/A11/A13/A15 absent; persisted at 1/min not 10 s; fails S1,S2,S3,S4,S5,S8,S9. |
| A16 per-exchange coverage — WEEX Futures | PARTIAL | NO | WeexFuturesMarketData.cs:103-162 → SnapshotCollector.cs:32-175 → market_snapshot; probe POLICY: weex-futures enabled, snapshot collect@30; probe A1 1422.4 rows/day | Polls at 30 s not 10 s; A10/A11/A13/A15 absent; instruments lacking a book/funding/OI sample are skipped whole with no gap row (WeexFuturesMarketData.cs:123-136); fails S1,S2,S3,S4,S5,S8,S9. |
| A16 per-exchange coverage — Hyperliquid | PARTIAL | NO | HyperliquidMarketData.cs:95-145 with HyperliquidWsFeed.cs:58-68 / HyperliquidBookFeed.cs:45-55 → SnapshotCollector.cs:32-175 → market_snapshot; probe POLICY: hyperliquid enabled, snapshot collect@10; probe A1 1438.5 rows/day | last_price is midPx and top-of-book may be 5 min old yet stamped now; A10/A11/A13/A15 absent; fails S1,S2,S3,S4,S5,S8,S9. |

**Not mentioned by the prior audit:**

- HEAD (7eece39) changes the health layer to agree with the lossy writer instead of the brief: RollupJob.cs:277 computes expected_count as least(3600 / greatest(interval_s, 60), 32767), i.e. a floor of 60 rows/hour, with the comment at :271-276 explaining that snapshot_count counts market_snapshot rows written once a minute. After this ships, the 5-of-6 discarded 10 s observations produce no shortfall anywhere in SQL. Deployed rows still carry the pre-fix values (probe HOURHEALTH: distinct expected_count = 120, 360), so the running system and HEAD currently disagree about what a complete hour is.
- received_at on Kraken rows is the exchange's clock, not ours: REST sets it from response.ServerTime (KrakenFuturesMarketData.cs:96,108) and WS from the frame's own time field (KrakenWsFeed.cs:176,179). The single time column that exists is therefore not a knowledge time, so the brief's known_from = received_at cannot even be back-derived for Kraken's rows — S2 is unrecoverable for existing history, not merely absent.
- Per-instrument absence is invisible and produces no gap row. WEEX skips any instrument without a book, funding or OI sample (WeexFuturesMarketData.cs:123-136) and Hyperliquid skips coins with no midPx or fresh book (HyperliquidMarketData.cs:113-121); the pass still returns success, so RecordGapAsync (ExchangeWorker.cs:388-406) writes nothing. Confirmed live: every collection_gap row has exchange_instrument_id NULL (probe GAP), and collector_run never populates exchange_instrument_id (insert at ExchangeWorker.cs:361-364 omits it; probe RUNCOLS: instrument_null = 23174 of 23174). This is exactly the T5 failure the brief names.
- Hyperliquid top-of-book (A3-A6) can be up to five minutes old while the row claims to be current: the REST fallback serves a cached l2Book with MaxAge = 5 minutes (HyperliquidBookFeed.cs:26,45-55), and the adapter stamps the resulting Ticker with DateTimeOffset.UtcNow (HyperliquidMarketData.cs:98,125). The book sample's own time is discarded, so the age is unrecoverable — the same class of defect as the WEEX-tail problem in §5 of the brief, on the venue nobody flagged for it.
- Exchange-supplied timestamps that would populate exchange_ts are dropped at the DTO layer on WEEX: the ticker's timestamp (Fixtures/weex/tickers.json:10) and the v3 bookTicker's time (Fixtures/weex/booktickers.json:2) are both in the payload and neither is bound (WeexDtos.cs:27-46). Adding the §4.1 column later will not recover them for any row written until then.
- Kraken inverse contracts are excluded rather than tagged: KrakenFuturesMarketData.cs:101 filters to the PF_ prefix, and the PI_XBTUSD row in Fixtures/kraken/tickers.json:44 is silently dropped. S4/T10 is satisfied today only by omission — the moment an inverse venue is added there is no unit tag to keep the columns apart.
- The only place 10 s-resolution ticker state ever exists is market_snapshot_latest, and that mutable cache is what every consumer actually reads: Endpoints.cs:158,169, ExchangeStore.cs:91,402,409, DashboardStore.cs:161, InstrumentStore.cs:77,147, AssetStore.cs:31,35,73. That is the brief's named trap (§5, "Reading *_latest from research code"), and it is the reason the WP1 loss is invisible from the UI: the screens look 10 s fresh while the archive is 1/min.
- market_snapshot history is on a live 90-day drop schedule against the brief's "Do not delete anything": retention_days = 90 is seeded at 0014_collections.sql:184 and RetentionJob.cs:53-83 drops whole partitions daily (drop table at :80-82), with no export first — the archive directories are absent on the host (probe ARCHIVE). Partitions present are only 2026_08/09/10 (probe PARTITIONS), so the oldest ticker history is already scheduled for destruction.

## B. Trades (WP3)

WP3 is not started: no trade operation on the adapter interface (src/CryptoSmithX.MarketData.Connectors/IExchangeMarketData.cs:26-54), no `create table trade` anywhere in migrations 0001-0017 (last DDL is 0017_collection_health.sql:46), and the probe confirms `trade`, `flow_1s/10s/1m/1h` and `feature_definition` ABSENT on test.
The gate is harder than policy: ExchangeWorker.cs:34 hard-codes the five implemented collections and DesiredCollectors (ExchangeWorker.cs:241-249) filters against it, so setting trades to `collect` would still start nothing.
Venue facts re-verified live today: Kraken's public WS `trade` feed carries uid, side, type(fill|liquidation|termination|block), seq, time, qty, price — i.e. B2-B5, B7, B8, B9 in one frame on a socket the hub already holds open.

| Item | Collected | Stored correctly | Evidence | Note |
|---|---|---|---|---|
| B1 transport is WS | NO | NO | KrakenWsFeed.cs:321 subscribes only "ticker"/"book"; KrakenWsFeed.cs:148-164 drops any other feed; HyperliquidWsFeed.cs:237 subscribes only l2Book, :126-129 returns on any non-l2Book channel; WeexFuturesMarketData.cs:36-44 declares rest-only; probe: `trade` ABSENT | No venue subscribes a trade channel and no tape table exists; S1-S9 all unmet (no table). |
| B2 trade id | NO | NO | IExchangeMarketData.cs:26-54 has no trade operation; no `create table trade` in 0001-0017 (grep over src/CryptoSmithX.Database/Migrations); probe: `trade` ABSENT on test | No message->parser->row path at any layer; S1-S9 unmet. |
| B3 event_time | NO | NO | IExchangeMarketData.cs:26-54; no trade.event_time column in any migration; ARCHIVE paths /csx-archive,/data/archive,/opt/cryptosmithx/archive ALL ABSENT (probe) | Not even the raw frames are retained, so each day's tape is unrecoverable, not merely unparsed; S1-S9 unmet. |
| B4 price | NO | NO | IExchangeMarketData.cs:26-54; no trade.price column in 0001-0017; Kraken WS trade frames carrying `price` are discarded at KrakenWsFeed.cs:148-164 | Venue-supplied price reaches the process on an open socket and is dropped; S1-S9 unmet. |
| B5 size with unit | NO | NO | IExchangeMarketData.cs:26-54; no trade.size column and no unit tag anywhere; contrast the documented unit comment on candles at KrakenFuturesMarketData.cs:11 | No size column exists, so S4 (unit tag, PI_ vs PF_ separation) cannot be met; S1-S9 unmet. |
| B6 notional | NO | NO | IExchangeMarketData.cs:26-54; no trade.notional column in 0001-0017; WEEX Trades channel serves notional verbatim as `v` (trade value) and is never subscribed (WeexFuturesMarketData.cs:36-44) | On WEEX notional is venue-supplied, not derived, and is still discarded; S1-S9 unmet. |
| B7 taker side | NO | NO | IExchangeMarketData.cs:26-54; no trade.taker_side column; Kraken WS `side` (docs.kraken.com futures-api/websocket/trade), HL WsTrade `side`, WEEX `m` maker-side flag — none consumed | All three venues expose side on the public feed; nothing is parsed; S1-S9 unmet. |
| B8 liquidation flag — Kraken | NO | NO | Kraken WS trade `type` = fill\|liquidation\|termination\|block (verified live, docs.kraken.com/api/docs/futures-api/websocket/trade); KrakenWsFeed.cs:321 subscribes only ticker/book; KrakenFuturesClient.cs:37-71 has no history endpoint | The flag is on the socket the process already holds; one string in SendFeedsAsync separates it from collection. |
| B8 liquidation flag — WEEX | N/A | N/A | WEEX contract Trades push fields verified live: e,E,s,d[T,t,p,q,v,m] — no liquidation field (weex.com/api-doc/contract/Websocket/public/Trades-Channel) | Public feed does not expose the fact; correctly N/A rather than a gap. |
| B8 liquidation flag — Hyperliquid | N/A | N/A | WsTrade verified live = coin, side, px, sz, hash, time, tid, users — no liquidation field (hyperliquid.gitbook.io websocket/subscriptions) | Public trades channel does not expose the fact; correctly N/A. |
| B9 exchange sequence — Kraken | NO | NO | Kraken WS trade carries `seq` ("subscription message sequence number", verified live); never subscribed (KrakenWsFeed.cs:321); `source_seq` occurs 0 times in all of src/ | A real per-feed sequence is offered and neither collected nor given a column; S1-S9 unmet. |
| B9 exchange sequence — WEEX | NO | NO | WEEX Trades `t` is an incremental trade id and the venue documents reconnect on out-of-order/duplicate `t` (verified live); WeexFuturesMarketData.cs:36-44 declares no ws transport at all | No sequence *field*, but the documented ordering identity exists and there is no WEEX WS client to read it. |
| B9 exchange sequence — Hyperliquid | N/A | N/A | WsTrade has no sequence; `tid` is documented live as a 50-bit hash of (buyer_oid, seller_oid), an identity not an ordering | No public sequence is documented; `tid` belongs to B2, not B9. |
| B10 Kraken Market History backfill (source=backfill, real known_from) | NO | NO | KrakenFuturesClient.cs:37-71 exposes only instruments/tickers/candles/funding/orderbook — no market-history executions path and no continuation-token loop; `known_from` occurs 0 times in all of src/ | Neither the fetch nor the provenance columns exist; S2 and S5 cannot be met anywhere in the repo today. |
| B11 per-exchange coverage — Kraken | NO | NO | KrakenFuturesMarketData.cs:41-48 declares no `trades` capability; KrakenWsFeed.cs:321; 0014_collections.sql:189,220 sets trades disabled; ExchangeWorker.cs:34 excludes trades from KnownCollectorCollections | B1-B7, B9, B10 all NO and B8 NO though Kraken uniquely provides it; nothing is stored. |
| B11 per-exchange coverage — WEEX | NO | NO | WeexFuturesMarketData.cs:36-44 (rest-only, no ws client exists); 0014_collections.sql:189,220; probe: trades disabled, `trade` ABSENT | B1-B7 and B9 NO, B8/B10 N/A; the brief's "WEEX after verification" step has no ADR and docs/adr does not exist. |
| B11 per-exchange coverage — Hyperliquid | NO | NO | HyperliquidMarketData.cs:50-57 declares no `trades` capability; HyperliquidWsFeed.cs:126-129,237; probe: trades disabled, `trade` ABSENT | B1-B7 NO, B8/B9/B10 N/A; the WS connection exists and only l2Book rides it. |

**Not mentioned by the prior audit:**

- The gate is code, not policy: ExchangeWorker.cs:34 hard-codes KnownCollectorCollections = [discovery, snapshot, depth, candles, funding] and DesiredCollectors (ExchangeWorker.cs:241-249) intersects policy with it, so an operator flipping exchange_collection.mode to 'collect' for trades starts nothing — a test pins exactly this at tests/CryptoSmithX.MarketData.Hub.Tests/CollectorSelectionTests.cs:58-69. The prior audit says only that policy disables trades, which understates the gap.
- No adapter declares a `trades` capability (KrakenFuturesMarketData.cs:41-48, HyperliquidMarketData.cs:50-57, WeexFuturesMarketData.cs:36-44), so ExchangeWorker.cs:436-458 writes we_implement=false for trades into exchange_collection_capability on every start — the deployed database itself records the WP3 gap as a fact.
- `known_from`, `source_seq` and `collection_run_id` appear ZERO times in all of src/ (grep, whole tree excluding obj/bin). The WP3 provenance vocabulary has no precedent anywhere in the repo, so B10's `source=backfill` with a real known_from is not a missing column on an existing table — it is a schema concept that does not exist yet.
- The tape loss compounds with the missing archive (probe: /csx-archive, /data/archive, /opt/cryptosmithx/archive ALL ABSENT). Kraken and Hyperliquid sockets are open right now and their trade frames are neither parsed nor stored raw, so every day without WP2 or WP3 is permanently unrecoverable — the prior audit noted the archive gap in section K but never joined it to section B.
- Quantitatively, seven of the eleven B items (B2 uid, B3 time, B4 price, B5 qty, B7 side, B8 type, B9 seq) arrive in a single Kraken WS frame on a connection the hub already maintains; the marginal collection cost is one extra string in KrakenWsFeed.SendFeedsAsync (KrakenWsFeed.cs:321). The prior audit spread the same facts across REST endpoints and never states that WP3's Kraken half is subscription-limited, not budget-limited.
- WEEX supplies notional directly as `v` (trade value) on its Trades channel, so B6 needs no derivation on that venue — no row in the prior audit mentions a venue-supplied notional, which matters for S4 unit tagging (venue quote units vs a computed price*size).
- The brief requires WEEX trades only 'after verification' and requires those verified exchange facts to be recorded in an ADR (brief sections 3/WP3 and 8), but docs/adr/ does not exist in the repo at all — so WEEX's B1/B9 verification step is not merely unfinished, it has no deliverable location.

## C. Book (WP4)

Judged against the repository at main (clean tree, HEAD f021bef) plus the supplied probes. No table, model, or column with book-topn shape exists anywhere in src, tests or migrations — grep for book_topn/bid_px/bid_sz/bid_n/is_snapshot/source_seq/known_from/collection_run_id returns zero hits outside the brief itself.
What exists of WP4 is entirely in-memory: a correct Kraken snapshot+delta book with seq validation and resync, and a full-snapshot HL l2Book cache; both are sampled at the 60 s depth cadence and reduced to six band sums before anything is written.

| Item | Collected | Stored correctly | Evidence | Note |
|---|---|---|---|---|
| C1 WS snapshot+deltas, REST only bootstrap/resync | PARTIAL | NO | Kraken snapshot+seq deltas: KrakenWsFeed.cs:158-163,194-229; per-instrument REST fallback still runs at depth@60 KrakenFuturesMarketData.cs:186-198. HL l2Book is full-snapshot only (HyperliquidWsFeed.cs:9-19,126-148) with an always-on REST cycler ExchangeWorker.cs:548-549 + HyperliquidBookFeed.cs:69-127. WEEX REST-only WeexFuturesMarketData.cs:37-44,223-233. Only market_snapshot.depth_bid_10bps..depth_ask_50bps (0001_initial.sql:194-200) is persisted, via SnapshotCollector.cs:146-162. | Deltas are persisted nowhere and REST is a standing 60 s poll rather than bootstrap/resync; fails S2,S4,S5,S9. |
| C2 sequence validation per instrument, gap -> collection_gap + resync | PARTIAL | NO | Kraken validates seq!=Seq+1 -> dirty (KrakenBookBuilder.cs:62-66) and resyncs (KrakenWsFeed.cs:224-228,266-277). collection_gap.cause allows 'ws_sequence_gap','ws_disconnected','resync' (0017_collection_health.sql:56-58) but the only writer is the REST loop path ExchangeWorker.cs:388-418; a WS gap only logs (KrakenWsFeed.cs:226). Probe GAP rows: 9 recent, all hyperliquid/snapshot/rate_limited, exchange_instrument_id NULL. | Seq validation exists on Kraken only and no WS gap or resync ever reaches collection_gap.exchange_instrument_id; fails S2,S5,S9. |
| C3 no fabricated states between gap and resync | PARTIAL | NO | In memory a dirty book yields no depth (KrakenBookBuilder.cs:106-109; test KrakenWsTests.cs:87-103). Persisted, SnapshotCollector.cs:156-158 selects market_snapshot_latest.depth_* into a new market_snapshot row while the book is untrusted, and DepthCollector.cs:64-84 only UPDATEs that mutable cache; probe DEPTHHIST kraken-futures 33917/33917 rows carry depth_bid_25bps. | The in-memory guard is real and tested, but history carries the last good depth forward with no gap row; fails S2,S3,S5,S8,S9. |
| C4 persisted top-N arrays bid_px/bid_sz/bid_n/ask_px/ask_sz/ask_n | NO | NO | Market/Depth.cs:9-16 carries six band sums and a timestamp, nothing per level; grep for book_topn\|bid_px\|ask_px\|bid_sz\|ask_sz\|bid_n over src,tests,deploy,docs returns zero hits; market_snapshot has only depth_bid_10bps..depth_ask_50bps (0001_initial.sql:194-200); probe: book_topn ABSENT on test. | No level array exists in any model, migration or table; fails S2,S4,S5,S9. |
| C5 n NULL where venue does not provide, never estimated | NO | NO | HL supplies an order count per level (Fixtures/hyperliquid/l2book_BTC.json:6-19) but HlLevel declares only Px/Sz (HyperliquidDtos.cs:35-39) and HyperliquidBookMath.cs:23-24 parses only those; Kraken WS levels genuinely have no count (KrakenWsDtos.cs:39-43); no n column exists in any table. | The one venue that provides order count has it discarded at parse, and nothing is estimated only because nothing is stored; fails S2,S5,S9. |
| C6 on-change persistence coalesced <=1 write/s, no fixed cadence, no heartbeat rows | NO | NO | DepthCollector.cs:44-95 is a fixed full pass writing only market_snapshot_latest (probe POLICY: depth collect@60); SnapshotCollector.cs:48-53,132-163 appends one market_snapshot row per instrument per minute unconditionally; probe A1 1438.5/1443.5/1422.4 rows per instrument per day against a required ~8640. | Fixed cadence with unconditional per-minute heartbeat rows and no change detection or coalescing anywhere; fails S2,S3,S5,S9. |
| C7 is_snapshot and seq on every row | NO | NO | Depth.cs:9-16 has no snapshot flag or sequence; Kraken's seq lives only in memory (KrakenBookBuilder.cs:170 SymbolBook.Seq) and HL has no seq at all (HyperliquidWsFeed.cs:9-19); market_snapshot columns 0001_initial.sql:177-203 carry neither, confirmed by probe SNAPSHOT COLUMNS. | Neither is_snapshot nor seq (nor source_seq) exists as a persisted column anywhere; fails S2,S5,S9. |
| C8 per-exchange coverage and subscription caps | PARTIAL | NO | Kraken subscribes ticker+book for every PF_ symbol in 200-chunks (KrakenWsFeed.cs:279-330, universe 276); HL one l2Book subscribe per live coin, no batch (HyperliquidWsFeed.cs:101-109,204-233, universe 177); WEEX has no WS build path at all (ExchangeWorker.cs:529-538; WeexFuturesMarketData.cs:37-44, universe 1006). No subscription registry, cap check or weight accounting exists in src; probe RUNCOLS: collector_run.request_weight NULL in all 23174 rows in 24 h. | Two of three venues subscribe their full universe but no cap or weight is tracked and WEEX book stays REST-only; fails S5,S9. |

**Not mentioned by the prior audit:**

- collection_gap already ships the exact vocabulary WP4 needs — cause values 'ws_sequence_gap', 'ws_disconnected', 'resync' and an exchange_instrument_id column (0017_collection_health.sql:50,56-58) — and no code path writes any of them; the C2 gap is entirely in the WS feeds, not in the schema.
- Kraken's REST cross-check is a second untracked blind window: KrakenWsFeed.cs:246-257 marks a book dirty and resyncs whenever WS mid drifts past the configured bps, and that interval is likewise persisted as carried-forward depth with no collection_gap row.
- WP4's "randomized order with jitter" for surviving REST polling is violated by both remaining pollers: DepthCollector.cs:24-30 orders by exchange_symbol and HyperliquidBookFeed.cs:134-138 orders ordinal, so the tail of every WEEX (1006 symbols x 50 ms) and HL (177 x 800 ms) pass is systematically staler — the exact WEEX-tail trap the brief names.
- Depth in the persistent history is not an independent observation: SnapshotCollector.cs:156-158 sources it from market_snapshot_latest, the mutable cache the brief says research code must never read, so market_snapshot inherits that cache's staleness semantics.
- Transport provenance of a stored depth value is unrecoverable: HyperliquidMarketData.cs:211-212 and KrakenFuturesMarketData.cs:188-197 silently swap WS and REST books into the same columns and market_snapshot has no source column, so no row can be attributed to ws vs rest.
- market_snapshot.bid_size and .ask_size are NOT NULL with no unit comment (0001_initial.sql:184-185), so the only persisted top-of-book sizes are zero-defaulted rather than null — probe S3ZERO shows ask_size=0 in 311 rows — and carry no contracts/base/quote tag.
- RollupJob.cs:236-238 averages market_snapshot.depth_bid_25bps over rows that include the carried-forward duplicates, so market_metric_hour.depth_bid_25bps_avg is weighted by repetition of a single stale measurement (probe METRICHOUR_DEPTH: weex 2010/23135, hyperliquid 247/4071 non-null).
- The discarded HL order count is irrecoverable rather than merely deferred: there is no raw archive writer in src (no ndjson/zstd/parquet code) and all three probed archive roots are absent, so WP2's rebuild path cannot restore n for any past day.

## D. Book features (WP4, WP9)

Rubric I applied for "stored": NO = the value reaches no persistent table at all; PARTIAL = it does reach one but the table fails S-criteria (ids listed). This is where I part company with the prior audit, which marked every D row NO.
S6 is judged only against derived tables (market_metric_hour); market_snapshot is a raw observation table, so S6 is N/A there and the absent book_feature_* layer is the real gap.
No exchange is 'enabled' by pristine migration (0005_exchange_admin.sql:27 sets every non-fake venue 'planned'), so no D row can reach collected=YES on the default-config gate alone.

| Item | Collected | Stored correctly | Evidence | Note |
|---|---|---|---|---|
| D1 spread bp | PARTIAL | PARTIAL | src/CryptoSmithX.MarketData.Hub/Rollups/MetricHour.cs:54-58,67 -> src/CryptoSmithX.MarketData.Hub/Rollups/RollupJob.cs:227-305 -> market_metric_hour.spread_bps_avg (DDL src/CryptoSmithX.Database/Migrations/0006_assets_and_metrics.sql:182); rollup loop hosted at src/CryptoSmithX.MarketData.Hub/Ingestion/ExchangeWorker.cs:72-73, default mode 'collect' at 0014_collections.sql:188; transient copy only at src/CryptoSmithX.MarketData.Api/Endpoints.cs:141-143 | Only an hourly mean over ~1/min inputs — no 1 s/10 s row and no median/p90/p95/p99/max; fails S1 (whole-row upsert RollupJob.cs:296-305), S2, S5, S6, S9. |
| D2 mid | NO | NO | Computed as a throwaway local only at src/CryptoSmithX.MarketData.Hub/Rollups/MetricHour.cs:54 and src/CryptoSmithX.MarketData.Connectors/Kraken/DepthMath.cs:47; no mid column in 0001-0017; Hyperliquid's own midPx is stored under a different meaning at src/CryptoSmithX.MarketData.Connectors/Hyperliquid/HyperliquidMarketData.cs:111-126 -> market_snapshot.last_price | Never persisted as a feature; reconstructable from market_snapshot.bid_price/ask_price at 1 min only; fails S2,S5,S6,S9. |
| D3 microprice | NO | NO | grep -riE 'microprice\|micro_price' over src and tests returns no hit; no column in src/CryptoSmithX.Database/Migrations/0001..0017; book_feature_1s absent (probe ABSENT list) | Not implemented anywhere; the four inputs sit in market_snapshot.bid_price/ask_price/bid_size/ask_size at 1/min, the feature does not; fails S2,S5,S6,S9. |
| D4 top-of-book sizes | PARTIAL | PARTIAL | src/CryptoSmithX.MarketData.Connectors/Kraken/KrakenWsFeed.cs:183-184, Weex/WeexFuturesMarketData.cs:145-146, Hyperliquid/HyperliquidBookMath.cs:31 -> Market/Ticker.cs:19-20 -> src/CryptoSmithX.MarketData.Hub/Ingestion/SnapshotCollector.cs:144-162 -> market_snapshot.bid_size / market_snapshot.ask_size (0001_initial.sql:184-185) | Raw sizes at 1/min only, no versioned book-feature row; NOT NULL columns turn an absent size into 0 (probe: ask_size=0 in 311 rows); fails S1,S2,S3,S4,S5,S9. |
| D5 depth +-5/10/25/50/100bp | PARTIAL | PARTIAL | src/CryptoSmithX.MarketData.Connectors/Kraken/DepthMath.cs:48-55 (10/25/50 only) -> src/CryptoSmithX.MarketData.Hub/Ingestion/DepthCollector.cs:59-84 -> market_snapshot_latest.depth_bid_25bps -> SnapshotCollector.cs:144-162 -> market_snapshot.depth_bid_25bps/depth_ask_25bps/depth_at (0001_initial.sql:194-200); probe DEPTHHIST confirms all three venues now reach history | +-5 and +-100 bp never computed, cadence 60 s (WEEX pass ~468 s), and every minute row re-copies the same latest reading; fails S1 (RetentionJob.cs:79-82 drops partitions), S2, S5, S8, S9. |
| D6 level count per band | NO | NO | src/CryptoSmithX.MarketData.Connectors/Kraken/DepthMath.cs:58-106 sums notional and counts nothing; the one venue that ships a per-level order count drops it at the DTO — HlLevel has only Px/Sz (src/CryptoSmithX.MarketData.Connectors/Hyperliquid/HyperliquidDtos.cs:33-39); no column in 0001-0017 | Neither level count nor order count survives the parser; fails S2,S5,S6,S9. |
| D7 largest level size and distance | NO | NO | No largest-level code (grep over src returns none); src/CryptoSmithX.MarketData.Connectors/Market/Depth.cs:9-16 reduces a book to six band sums; book_topn absent (probe ABSENT list) | Not computed, and no level array is retained that would let it be computed later; fails S2,S4,S5,S6,S9. |
| D8 HHI concentration +-50bp | NO | NO | grep -riE 'hhi\|concentration' over src and tests returns no hit; per-level data is discarded at Market/Depth.cs:9-16 and DepthMath.cs:58-106; book_feature_1s absent (probe) | No computation and no per-level input retained; fails S2,S5,S6,S9. |
| D9 imbalance +-5/10/25/50bp | NO | NO | grep -ri 'imbalance' over src returns no hit (only ViewModels.cs SearchHit false positives); no column in 0001-0017; book_feature_1s absent (probe) | Never computed; +-10/25/50 bp imbalance is reconstructable from market_snapshot.depth_bid_*/depth_ask_* at 1/min, +-5 bp is not; fails S2,S5,S6,S9. |
| D10 book slope | NO | NO | grep -ri 'slope' over src and tests returns no hit; the only book representation is the six band sums at Market/Depth.cs:9-16 | Not computed and no level curve is stored to fit one; fails S2,S5,S6,S9. |
| D11 cost-to-execute ladder both sides | NO | NO | grep -riE 'slippage\|cost_to_execute' over src hits only the prose in src/CryptoSmithX.Database/Migrations/0014_collections.sql:185; DepthMath.cs:48-55 yields band notional sums, from which no average fill price for a ladder step can be recovered | No ladder, no average fill price, no slippage bp — the seed text claims depth answers this and the schema cannot; fails S2,S4,S5,S6,S9. |
| D12 book persistence 1s/10s | NO | NO | DepthCollector.cs:64-84 overwrites market_snapshot_latest in place, so no two consecutive book states coexist; book_topn and book_feature_1s absent (probe ABSENT list) | Requires a top-N time series that is never written; fails S2,S5,S6,S9. |
| D13 update count per bucket | NO | NO | market_metric_hour.snapshot_count (DDL 0006_assets_and_metrics.sql:185, set from MetricHour.cs:70) counts snapshot history rows, not book updates; collector_run carries no per-instrument depth row (probe RUNCOLS: instrument_null=23174 of 23174) | No book-update counter exists at any cadence; fails S1,S2,S5,S6,S9. |
| D14 cadences 1s/10s/1m/1h distributions | PARTIAL | PARTIAL | SnapshotCollector.cs:48-53 gates history to one row per minute -> market_snapshot (probe A1: 1422-1443 rows/instrument/day vs 8640 required); RollupJob.cs:227-305 -> market_metric_hour hourly means; no percentile column in any migration | Only 1 m raw and 1 h means exist; the 1 s and 10 s layers and every distribution are absent; fails S1,S2,S5,S6,S9. |
| D15 derived_version + feature_definition | NO | NO | grep -riE 'derived_version\|feature_definition' over src and tests returns no hit; the one derived microstructure table carries no version column (0006_assets_and_metrics.sql:177-188, extended at 0017_collection_health.sql:86-88); feature_definition absent (probe ABSENT list) | No versioned derived row and no definition registry to reference; fails S2,S5,S6,S9. |

**Not mentioned by the prior audit:**

- The hourly depth feature double-counts one measurement: SnapshotCollector.cs:152-159 copies the same market_snapshot_latest depth reading into every minute row, and MetricHour.cs:68-69,74-89 averages all rows while ignoring depth_at — on WEEX one ~468 s depth pass is therefore weighted ~8x in market_metric_hour.depth_bid_25bps_avg, so the 'hourly average depth' is a mean over duplicated observations, not over distinct measurements.
- No depth outage is ever recorded as a gap for D5: ExchangeWorker.cs:388-417 opens a collection_gap only when a whole pass throws, only once per failure run, and never with exchange_instrument_id (probe: all 9 gap rows have exchange_instrument_id NULL and are snapshot/rate_limited). DepthCollector.cs:60 silently skips an instrument whose book comes back null, leaving the previous depth in market_snapshot_latest to be re-copied into the next minute row — the exact 'no rows plus a gap record' rule of invariant 1 inverted into a carried-forward value with no gap.
- Hyperliquid is the one venue that ships per-level order counts and they are dropped at the DTO boundary: HyperliquidDtos.cs:33-39 HlLevel exposes only Px and Sz. The prior audit noted this under C5 but did not carry it into D6, where it is the reason level/order count per band can never be backfilled from the archive-less pipeline.
- Mid, microprice and +-10/25/50 bp imbalance (D2, D3, D9) are exactly reconstructable from columns already stored — market_snapshot.bid_price/ask_price/bid_size/ask_size and depth_bid_*/depth_ask_* — at 1-minute resolution. What is lost for these three is resolution, not the field, which puts them in a different remediation class from D7/D8/D10/D11/D12, where the per-level input itself is discarded and is unrecoverable.
- The only persistent home of any D feature is mutable by design: RollupJob.cs:296-305 upserts the whole market_metric_hour row on every 60 s pass and stamps updated_at, so a corrected or late snapshot silently rewrites history in place and there is no known_from by which the earlier value could be read back — the derived layer for D1/D5 cannot answer 'what did we believe at time t'.

## E. Flow features (WP3)

No trade tape exists: `IExchangeMarketData` has no trade operation (src/CryptoSmithX.MarketData.Connectors/IExchangeMarketData.cs:10-54), the WS feeds subscribe only ticker/book (KrakenWsFeed.cs:321, HyperliquidWsFeed.cs:237), and probe reports `trade` and all `flow_*` relations ABSENT.
The single flow-shaped fact in the system is Hyperliquid's venue-reported per-minute bar trade count landing in market_candle.trade_count; everything else in WP3's derived layer is unbuilt.
`trades` and `liquidations` are disabled by default policy AND structurally unstartable — `KnownCollectorCollections` omits them (ExchangeWorker.cs:34).

| Item | Collected | Stored correctly | Evidence | Note |
|---|---|---|---|---|
| E1 trade count | PARTIAL | NO | HyperliquidDtos.cs:61 ("n") -> HyperliquidMarketData.cs:171 -> CandleCollector.cs:86-99 -> market_candle.trade_count (0001_initial.sql:228); Kraken/WEEX write null (KrakenFuturesMarketData.cs:157, WeexFuturesMarketData.cs:191); no trade tape (IExchangeMarketData.cs:10-54) | Only Hyperliquid's venue-reported 1m bar count — no per-trade tape, no buy/sell split, none for Kraken/WEEX; fails S1,S2,S3,S5,S6,S9. |
| E2 buy/sell count | NO | NO | No trade input (IExchangeMarketData.cs:10-54); trades disabled (0014_collections.sql:189,220) and unstartable (ExchangeWorker.cs:34); probe: flow_1s/flow_10s/flow_1m/flow_1h ABSENT | No taker side is parsed anywhere, so no side split can exist; fails S2,S5,S6,S9. |
| E3 buy/sell notional | NO | NO | No trade input (IExchangeMarketData.cs:10-54); only unsigned aggregates exist — market_candle.volume (0001_initial.sql:227, base units) and market_snapshot.turnover_24h; probe: flow_* ABSENT | Total 24h/bar volume is not a buy/sell notional and carries no side or unit tag; fails S2,S4,S5,S6,S9. |
| E4 net taker notional and CVD | NO | NO | grep for cvd/net taker across src+tests returns nothing; no trade input (IExchangeMarketData.cs:10-54); probe: flow_* ABSENT | Neither computed nor persisted anywhere; fails S2,S4,S5,S6,S9. |
| E5 VWAP | NO | NO | grep for vwap across src+tests returns nothing; no trade input (IExchangeMarketData.cs:10-54); probe: flow_* ABSENT | Neither computed nor persisted anywhere; fails S2,S5,S6,S9. |
| E6 realized vol from trades | NO | NO | grep for volatil/stddev across src+tests returns nothing; RollupJob.cs:155-197 carries only OHLCV+count; probe: flow_* ABSENT | No variance/return computation of any kind exists; fails S2,S5,S6,S9. |
| E7 trade-size distribution | NO | NO | grep for percentile/median/p90/p99 across src+sql returns nothing; no trade sizes are parsed (IExchangeMarketData.cs:10-54); probe: flow_* ABSENT | Mean/median/p90/p99/max absent and no size unit tag exists; fails S2,S4,S5,S6,S9. |
| E8 inter-trade time | NO | NO | No per-trade event_time is ever parsed (IExchangeMarketData.cs:10-54); probe: trade and flow_* ABSENT | Without trade timestamps the median/max gap is uncomputable; fails S2,S5,S6,S9. |
| E9 burstiness | NO | NO | grep for burstiness/histogram across src returns nothing; no trade input (IExchangeMarketData.cs:10-54); probe: flow_* ABSENT | Neither computed nor persisted anywhere; fails S2,S5,S6,S9. |
| E10 liquidation count/notional | NO | NO | 0014_collections.sql:191 seeds liquidations default_mode='disabled' and :222 backfills mode='disabled'; ExchangeWorker.cs:34 omits it from KnownCollectorCollections; CollectorSelectionTests.cs:66-68 asserts it is never started | Kraken public history does expose liquidation type, so this is unbuilt rather than unavailable; fails S2,S4,S5,S6,S9. |
| E11 cadences 1s/10s/1m/1h | PARTIAL | NO | 1m: CandleCollector.cs:86-99 -> market_candle.trade_count (timeframe=1); 1h: RollupJob.cs:167-197 sums into market_candle.trade_count (timeframe=60, seeded 0014_collections.sql:198); no 1s/10s layer exists; probe: flow_1s/flow_10s ABSENT | Only Hyperliquid's count reaches 1m and 1h; 1s and 10s do not exist for any venue; fails S1,S2,S3,S5,S6,S9. |
| E12 derived_version + feature_definition | NO | NO | grep for derived_version/feature_definition across src returns zero hits; probe: feature_definition ABSENT; existing derived rows carry none — market_candle (0001_initial.sql:218-235) and market_metric_hour (0006_assets_and_metrics.sql:177-190 + 0017_collection_health.sql:86-88) | Not only are flow tables missing — the derived layer that does exist is entirely unversioned; fails S2,S5,S6,S9. |

**Not mentioned by the prior audit:**

- The one live trade-count path invents zeros: HyperliquidDtos.cs:61 maps "n" to a non-nullable int, so a missing count becomes market_candle.trade_count = 0 rather than NULL (HyperliquidMarketData.cs:171 -> CandleCollector.cs:89) — invariant 2 / S3 violated inside the only flow-ish series that exists.
- Enabling `trades` or `liquidations` in policy would change nothing: ExchangeWorker.cs:34 hard-codes KnownCollectorCollections = discovery/snapshot/depth/candles/funding and DesiredCollectors filters against it (ExchangeWorker.cs:242-248), with CollectorSelectionTests.cs:57-69 asserting that mode='collect' on trades still yields no loop — the admin toggle is inert, not merely off.
- Conversely, the aggregation side does honour NULL != 0: RollupJob.cs:172-173 and Rollup.cs:63-65 null the 1h trade_count unless every source minute has one (RollupTests.cs:80). The S3 defect is in the Hyperliquid parser, not in the rollup — prior credited neither fact.
- The only 1h count is bucket-start-addressable and mutable: RollupJob.cs:184-197 writes market_candle keyed by open_time with no known_from and `on conflict do update` rewriting the whole bucket whenever a late 1m bar arrives — the brief's first look-ahead trap (section 5) plus an unversioned overwrite, so a replay cannot reproduce what it saw.
- Budget exposure of the sole trade-count source went unmentioned: CandleCollector.cs:48-52 targets every non-delisted collecting instrument each pass at candles@60, i.e. Hyperliquid 177 × weight 20 ≈ 3540/min against a documented 1200/min IP budget (brief section 1), and the probe's 9 recent collection_gap rows are all hyperliquid/rate_limited — the one venue that reports trade counts is the one being throttled.

## F. Candles (WP5)

market_candle is real, partitioned and never rotated (Partitions.cs:13; RetentionJob.cs:7-8,60-61), and all three live adapters declare and run the candles loop, so collection is genuinely happening. Every WP5 storage requirement is absent from the schema: grep across all 17 migrations returns zero hits for close_time, is_final, source, known_from, price_type, derived_version, collection_run_id, source_seq. Both writers (CandleCollector.cs:90-97, RollupJob.cs:190-198) mutate rows in place, so no candle row can be judged stored=YES.

| Item | Collected | Stored correctly | Evidence | Note |
|---|---|---|---|---|
| F1 lazy batch fetch with overlap, not per-instrument-per-minute polling | PARTIAL | NO | CandleCollector.cs:67-75 (re-asks the newest stored minute, ranged from/to) -> KrakenFuturesMarketData.cs:131-160 / HyperliquidMarketData.cs:147-175 / WeexFuturesMarketData.cs:164-190 -> market_candle.open_time (CandleCollector.cs:84-100); cadence 60 s at 0014_collections.sql:186, loop per exchange at ExchangeWorker.cs:258,266 | Overlap and ranged requests exist, but it is still one REST call per instrument per 60 s pass with no batch endpoint, no 15-60 min lazy cadence and no weight budget; stored fails S1,S2,S4,S5,S7,S9. |
| F2 Hyperliquid at least hourly (5000-candle cap) | PARTIAL | NO | HyperliquidClient.cs:63-66 (candleSnapshot) -> HyperliquidMarketData.cs:147-172 -> market_candle.open_time/open/high/low/close/volume/trade_count (CandleCollector.cs:84-100); HL enabled and candles collect@60 per probe POLICY line; hard floor at CandleCollector.cs:43,69-73 with backfill_hours=3 at 0014_collections.sql:195 | Cadence beats hourly, but the 3 h floor clamps `from` even when a newer bar exists, forfeiting the ~3.5 days HL still serves after any longer outage; stored fails S1,S2,S4,S5,S7,S9. |
| F3 Kraken mark-price candles with price_type=mark | NO | NO | KrakenFuturesClient.cs:61-62 hardcodes `{_chartsUrl}/trade/{symbol}/1m?from=&to=`; grep for tick_type/price_type across src returns zero hits; probe CANDLE COLUMNS has no price_type; PK (exchange_instrument_id,timeframe,open_time) at 0001_initial.sql:232 cannot hold two price types | No mark request, no parser, no column, and the primary key would collide if one were added; fails S1,S2,S4,S5,S7,S9. |
| F4 columns open_time/close_time/is_final/source/known_from | PARTIAL | NO | market_candle DDL 0001_initial.sql:218-235 has open_time (221) and updated_at (230) only; probe CANDLE COLUMNS confirms; HL close time "T" is present in the response and deliberately not mapped (HyperliquidDtos.cs:50-53,55); Candle.cs:8-16 carries no close/final/source/known field | Only open_time of the five exists; close_time, is_final, source and known_from are neither parsed nor stored; fails S1,S2,S4,S5,S7,S9. |
| F5 unclosed bars never canonical | YES | NO | Forming bar dropped in every live adapter: KrakenFuturesMarketData.cs:144, HyperliquidMarketData.cs:158, WeexFuturesMarketData.cs:176; rollup writes only closed windows at RollupJob.cs:181; persisted to partitioned, never-rotated market_candle (Partitions.cs:13, RetentionJob.cs:7-8) | Guard is uniform and enabled, but the table has no is_final, rows are overwritten later (CandleCollector.cs:90-97) and derived bars with bar_count<timeframe are canonical and unmarked (0001_initial.sql:229,242-244); fails S1,S2,S4,S5,S7,S9. |
| F6 tf>1 versioned and rebuildable | PARTIAL | NO | derived_timeframes 5,15,60,240,720,1440 at 0014_collections.sql:198 -> RollupJob.cs:140-211 aggregates and upserts into market_candle (184-198); no derived_version column (probe CANDLE COLUMNS), feature_definition ABSENT per probe; cold start capped at 1 day (RollupJob.cs:43), bite capped at 30 min (RollupJob.cs:37); grep for `rebuild` finds only a doc comment | Derived bars are produced and opportunistically repaired, but carry no version and have no rebuild entrypoint; fails S1,S2,S5,S6,S7,S9. |

**Not mentioned by the prior audit:**

- The 3 h backfill floor is a hard clamp, not just a first-run bound: CandleCollector.cs:69-73 lowers `from` to now-3h even when a newer stored bar exists, so an outage longer than 3 h permanently loses candles the venue would still serve (HL keeps ~3.5 days behind the 5000-bar cap).
- Per-symbol candle failures are swallowed at CandleCollector.cs:110-114 and the pass still reports success as long as one symbol wrote, so RecordGapAsync (ExchangeWorker.cs:388-417) never opens a collection_gap for that instrument — probe confirms all 9 gap rows are hyperliquid/snapshot and zero are candles, and the insert at ExchangeWorker.cs:410 omits exchange_instrument_id entirely (probe: all NULL).
- collector_run.exchange_instrument_id and request_weight were added by 0017_collection_health.sql:24-28 explicitly for per-instrument polls like candles, but the writer at ExchangeWorker.cs:359-371 never populates them (probe RUNCOLS: instrument_null=23174/23174, request_weight_null=23174/23174), so HL's candle weight (177 x 20 = 3540/min against a 1200/min budget) is unmeasurable from the data.
- Hyperliquid's candleSnapshot response carries a close time under key "T" and HyperliquidDtos.cs:50-53 names it while deliberately leaving it unmapped — F4's close_time is available from at least one venue today and thrown away.
- The fake exchange generates synthetic bars through the same collector into the same table (FakeExchangeMarketData.cs:152-185); with no `source` column, synthetic and real candles are indistinguishable in market_candle, and 0005_exchange_admin.sql:27 seeds fake as the one 'enabled' exchange.
- HL's trade count is a non-nullable int defaulting to 0 (HyperliquidDtos.cs:60), so a missing "n" would be stored as 0 rather than NULL in market_candle.trade_count — an S3 hole in the one venue that supplies the counter.
- The forming-bar guard compares against the collector's own clock (`to` = CandleCollector.cs:42) with no clock-offset measurement anywhere (collector_run.clock_offset_ms never written), so a locally fast clock can admit a still-forming bar which is then silently overwritten by the next pass's upsert.
- S8 actually passes for candles: the only readers (InstrumentStore.cs:153-163 and 232-241) return raw rows plus an explicit LastCandleAge, with no carried-forward value and no view or function over market_candle.

## G. Funding (WP6)

funding_rate_history exists (0006_assets_and_metrics.sql:154-159, schema v17) and is genuinely append-only, but it is a 3-column table: no known_from, received_at, source or run id.
funding_schedule and market_snapshot_10s are ABSENT on the deployed DB (probe ABSENT list); the only schedule fact stored anywhere is the mutable exchange_instrument.funding_interval_hours.
Predicted funding and next funding time are served by Kraken and discarded at the DTO layer — nothing downstream could persist them.

| Item | Collected | Stored correctly | Evidence | Note |
|---|---|---|---|---|
| G1 realized funding history with known_from | PARTIAL | NO | src/CryptoSmithX.MarketData.Connectors/Kraken/KrakenFuturesMarketData.cs:163-184, Weex/WeexFuturesMarketData.cs:199-221, Hyperliquid/HyperliquidMarketData.cs:178-194 -> src/CryptoSmithX.MarketData.Hub/Ingestion/FundingCollector.cs:80-88 -> funding_rate_history.exchange_instrument_id/.funding_time/.rate (DDL src/CryptoSmithX.Database/Migrations/0006_assets_and_metrics.sql:154-159 — those three columns are the whole table) | History persists and is append-only, but known_from/received_at is never captured (insert passes only Id/FundingTime/Rate) and real venues default to 'planned' (0005_exchange_admin.sql:27); fails S2,S3,S5,S9. |
| G2 funding_schedule SCD2 (interval, settlement times, sign convention, rate basis) | PARTIAL | NO | Only the interval is ever observed, and only on WEEX (Weex/WeexFuturesMarketData.cs:71-75, collectCycle else invented 8h); Kraken and HL hardcode 1h (KrakenFuturesMarketData.cs:25,76; HyperliquidMarketData.cs:25,85) -> Hub/Ingestion/DiscoveryCollector.cs:93-124 -> exchange_instrument.funding_interval_hours (0001_initial.sql:82, NOT NULL check>0), overwritten every pass at DiscoveryCollector.cs:115; funding_schedule ABSENT (probe ABSENT relations) | No settlement-time, sign-convention or rate-basis column exists (semantics live only in comments 0001_initial.sql:107-108 and 0006:161-163); fails S1,S2,S3,S5,S7,S9. |
| G3 predicted funding and next funding time in the 10s snapshot | NO | NO | Kraken serves both (Fixtures/kraken/tickers.json:20 fundingRatePrediction; Fixtures/kraken-ws/ticker.json:7-8 relative_funding_rate_prediction and next_funding_rate_time) but no DTO member exists (Kraken/KrakenDtos.cs:36-55, Kraken/KrakenWsDtos.cs:10-28) and Market/Ticker.cs:13-27 has no such field; market_snapshot.funding_rate is the only funding column (0001_initial.sql:188; probe SNAPSHOT COLUMNS) and market_snapshot_10s is ABSENT (probe) | Both values are dropped at deserialisation, so there is nothing to persist; fails S2,S5,S9. |

**Not mentioned by the prior audit:**

- Kraken and Hyperliquid funding intervals are hardcoded constants (KrakenFuturesMarketData.cs:25,76; HyperliquidMarketData.cs:25,85), so the one schedule fact that does reach a table is an assumption, not a collected observation, for 2 of 3 live venues.
- funding_rate_history.rate is NOT NULL (0006_assets_and_metrics.sql:157) and both WEEX and HL history DTOs default the rate to the string "0" (WeexDtos.cs:70, HyperliquidDtos.cs:68) — a missing rate becomes a real zero payment, a direct S3 violation on G1's own table.
- FundingCollector.cs:94-99 counts per-symbol failures and only throws when every symbol fails, and collection_gap is keyed (exchange_code, collector) with no instrument (ExchangeWorker.cs:408-417) — a single instrument's funding outage is unrecordable by construction, so G1 can never satisfy S3's gap-record half.
- funding_rate_history genuinely passes S1: the only write is `insert ... on conflict do nothing` (FundingCollector.cs:80-88) and RetentionJob drops only market_snapshot_YYYY_MM partitions (RetentionJob.cs:60-83) — the prior audit's blanket storage note does not credit this.
- WEEX history is capped at 100 rows with no pagination lever (WeexDtos.cs:65-67) while the collector resumes from max(stored funding_time) (FundingCollector.cs:20-22,71-75), so an outage longer than the venue window leaves a permanent, unmarked hole in the realized series.
- exchange_instrument.funding_interval_hours is a smallint in whole hours (0001_initial.sql:82) and WEEX floors collectCycle with Math.Max(1, cycle/60) (WeexFuturesMarketData.cs:73-74) — any sub-hour or non-hour-multiple funding cycle is unrepresentable and silently rounded.
- Kraken WS delivers next_funding_rate_time on every ticker frame (Fixtures/kraken-ws/ticker.json:8), which is the exact settlement-time input WP6/G2 asks for; KrakenWsDtos.cs:10-28 declares no member for it, so the schedule gap is a parser gap, not a venue gap.

## H. Instrument lifecycle (WP7)

WP7 is unimplemented: neither instrument_spec_version nor instrument_status_event exists in migrations 0001-0017 or on the deployed schema (probe: both ABSENT), and no code references either name. Everything lifecycle-related lives in the single mutable row exchange_instrument, rewritten by DiscoveryCollector.cs:91-166 on every pass. Judged per table, exchange_instrument fails S1,S2,S3,S4,S5,S7,S8,S9 (S6 N/A), so every stored verdict below is NO.

| Item | Collected | Stored correctly | Evidence | Note |
|---|---|---|---|---|
| H1 identity (exchange, native id, first_seen); symbol is a label | PARTIAL | NO | Instrument.cs:20-32 carries no native id → DiscoveryCollector.cs:91-147 → exchange_instrument.exchange_code/exchange_symbol/first_seen_at (0001_initial.sql:71-72,86); identity is unique(exchange_code, exchange_symbol) at 0001_initial.sql:92 | Symbol IS the key (not a label), first_seen is not part of it, row is mutated in place; HL universe index never read (HyperliquidDtos.cs:8-13); fails S1,S2,S3,S4,S5,S7,S8,S9. |
| H2 instrument_spec_version SCD2 (tick/lot/min notional/max leverage/multiplier/funding interval/status) | PARTIAL | NO | KrakenFuturesMarketData.cs:63-79, WeexFuturesMarketData.cs:84-97, HyperliquidMarketData.cs:81-89 → DiscoveryCollector.cs:103-115 overwrite exchange_instrument.price_step/qty_step/min_qty/min_notional/contract_multiplier/funding_interval_hours (0001_initial.sql:76-85); instrument_spec_version ABSENT (probe; absent from migrations 0001-0017) | Max leverage collected nowhere (HL publishes it, Fixtures/hyperliquid/meta.json:6, dropped at HyperliquidDtos.cs:8-13), WEEX invents 8 h (WeexFuturesMarketData.cs:73-75); no valid_from/valid_to/known_from; fails S1,S2,S3,S4,S5,S7,S8,S9. |
| H3 instrument_status_event (listed/halted/reduce-only/delisting announced/delisted/re-listed) | PARTIAL | NO | Only Kraken derives a venue status (KrakenFuturesMarketData.cs:207-210); WEEX/HL hardcode Trading (WeexFuturesMarketData.cs:96, HyperliquidMarketData.cs:88) → DiscoveryCollector.cs:117-121,154-166 overwrite exchange_instrument.status/status_changed_at (0001_initial.sql:84-85); instrument_status_event ABSENT (probe) | Only halted/post_only reach the store and only from Kraken; reduce_only and delisting-announced never collected, delisted is inferred not observed; fails S1,S2,S3,S4,S5,S7,S8,S9. |
| H4 first_seen/last_seen/delisted_at never overwritten | PARTIAL | NO | DiscoveryCollector.cs:97,102 stamp first_seen_at/last_seen_at (first_seen_at is absent from the DO UPDATE set, :103-124, so it survives); :122 overwrites last_seen_at every pass; :154-166 rewrite status_changed_at; no delisted_at column exists (0001_initial.sql:69-93; 0006_assets_and_metrics.sql:97-99; repo-wide grep finds none) | Only first_seen_at is immutable; last_seen_at and status_changed_at are overwritten and delisted_at does not exist; fails S1,S2,S3,S4,S5,S7,S8,S9. |
| H5 discovery cadence <=60 min | PARTIAL | NO | 0014_collections.sql:183 seeds discovery mode collect / 3600 s (probe: discovery collect@3600, all three venues); hosted at ExchangeWorker.cs:255,263 with an extra pass at start :186-188; delay = Jitter(DelayFor(interval, failures)) computed AFTER the pass at CollectorLoop.cs:143, jitter 0.9-1.1x at :159-160, backoff up to 5x at :29,81 | Start-to-start = pass duration + up to 1.1x3600 s, so >60 min about half the time and up to ~5 h after 3 failures; the pass writes only in-place upserts, fails S1,S2,S5,S7,S9. |

**Not mentioned by the prior audit:**

- Hyperliquid's explicit delist signal is thrown away: HyperliquidMarketData.cs:66-70 skips any coin with isDelisted, so the venue's own statement never reaches the store, and the delisting is instead invented ~3 h later by the last_seen_at heuristic (DiscoveryCollector.cs:150-166). Kraken does the same with isExpired at KrakenFuturesMarketData.cs:57-61.
- WEEX fabricates delistings from an absence of trades: WeexFuturesMarketData.cs:61,258 drop any contract whose ticker shows last<=0 or volume_24h<=0, so a still-listed but untraded contract stops being seen and DiscoveryCollector.cs:154-166 writes status='delisted' after 3 passes (0014_collections.sql:197). That is a lifecycle event with no exchange evidence behind it — brief invariant 1.
- raw_json is not the venue payload for two of three venues, so it is no fallback for the dropped spec fields: HL re-serializes three fields (HyperliquidMarketData.cs:228-230, losing maxLeverage which meta.json:6 actually returns) and WEEX six (WeexFuturesMarketData.cs:262-264, losing contract_val/maxOrderSize/forwardContractFlag). Only Kraken stores the raw element text (KrakenFuturesClient.cs:51), contradicting the column comment at 0001_initial.sql:89.
- A relist collapses two listings into one row: the key is (exchange_code, exchange_symbol) (0001_initial.sql:92), so a reappearing symbol flips status back to 'trading' and moves status_changed_at (DiscoveryCollector.cs:117-121), destroying the only surviving delisting timestamp, while first_seen_at still dates from the first listing. The dead interval becomes unqueryable — exactly the survivorship/look-ahead trap in brief section 5.
- Even the inferred delist timestamp is wrong by construction: status_changed_at is set to the discovery pass's own now (DiscoveryCollector.cs:158), i.e. up to 3 discovery intervals (~3 h, 0014_collections.sql:197) after the instrument actually vanished, and the cutoff is computed from the configured interval rather than the observed one (DiscoveryCollector.cs:152-153).
- No adapter can ever emit reduce_only: InstrumentStatus.ReduceOnly exists in the enum (InstrumentStatus.cs:12) and in the CHECK (0001_initial.sql:84) but no code path produces it, so one of the six required WP7 event types is unreachable by design.
- Nothing tests lifecycle: tests/ contains no assertion on first_seen/last_seen/delist behaviour, and the one discovery test asserts the opposite of what WP7 wants — HyperliquidMarketDataTests.cs:26,58 assert that a delisted coin is excluded from discovery output.

## I. Collection health (WP8)

WP8 shipped as two commits (868ae00, 2ae2745) plus a fix (7eece39). The tables exist and are hosted (Program.cs:32), but the writer fills roughly half the declared columns, and the live expected_count values match the PRE-fix formula only — so the repo's fix at RollupJob.cs:277 is in no live row.
Nothing in the repository ever reads expected_count, snapshot_count or gap_seconds back; the only reader of market_metric_hour omits all three (InstrumentStore.cs:166-175).
No test covers collection_gap, CauseOf, or the health block (tests/ grep: zero hits), so brief T5 has no test.

| Item | Collected | Stored correctly | Evidence | Note |
|---|---|---|---|---|
| I1 collection_run complete (feed, exchange, instrument, transport, started/finished, status, HTTP status, latency, weight, clock offset) | PARTIAL | PARTIAL | src/CryptoSmithX.Database/Migrations/0009_collector_runs.sql:20-29 and 0017_collection_health.sql:24-29 declare collector_run.{exchange_code,collector,started_at,duration_ms,ok,error,items,exchange_instrument_id,transport,http_status,request_weight,clock_offset_ms}; sole writer src/CryptoSmithX.MarketData.Hub/Ingestion/ExchangeWorker.cs:359-371 inserts only 8 of them; probe RUNCOLS instrument_null=23174/23174, http_status_null=23174, request_weight_null=23174 | Feed/exchange/started+duration/status/latency populated; instrument, HTTP status, weight, clock offset never written and transport is the hardcoded default CollectorLoop.cs:17; fails S2 (no known_from), S5 (no source tag). |
| I2 collection_gap complete (instrument, feed, gap_start, gap_end, cause taxonomy) | PARTIAL | PARTIAL | collection_gap declared 0017_collection_health.sql:46-64 with an 8-value cause check at :53-61; sole insert ExchangeWorker.cs:408-417 omits exchange_instrument_id; CauseOf at ExchangeWorker.cs:425-434 can emit only rate_limited/timeout/exchange_maintenance/error; probe GAP: 9 rows, all exchange_instrument_id NULL | Only exchange+collector-level REST loop failures; ws_sequence_gap/ws_disconnected/resync/collector_down unreachable and per-instrument gaps never written; closure is an UPDATE (ExchangeWorker.cs:392-398) so fails S1, plus S2, S5. |
| I3 every rollup bucket carries expected_count, actual_count, gap_count, max_gap_seconds | PARTIAL | NO | market_metric_hour.expected_count/gap_seconds added 0017_collection_health.sql:86-88, written at RollupJob.cs:265-305 alongside snapshot_count; the other rollup layer, market_candle timeframes at RollupJob.cs:184-198, carries no health column (probe CANDLE COLUMNS); no gap_count or max_gap_seconds exists anywhere in the schema; probe HOURHEALTH expected_count_null=32096/33554 | 2 of 4 quantities on 1 of 2 rollup layers, 4% populated; fails S1 (upsert RollupJob.cs:296-305), S2 (hour_time is bucket start, 0006:179; no known_from), S3 (gap_seconds coalesce 0, :279), S5, S6. |
| I4 NTP/clock offset per run | NO | NO | collector_run.clock_offset_ms declared 0017_collection_health.sql:29 with comment :40-42; repo-wide grep for ntp/chrony/timedatectl/clock offset over src, deploy and docs returns only that migration; ExchangeWorker.cs:359-371 does not bind the column; probe RUNCOLS clock_offset_null=23174/23174 | Column-only: no clock source is queried and no NTP state is measured or monitored anywhere in the repo or deploy. |
| I5 market-silent vs collector-blind separable by one query, cite it | NO | NO | No such query exists: only reader of market_metric_hour is InstrumentStore.cs:166-175 and it selects OI/funding/spread only; a hand-written SELECT snapshot_count,expected_count,gap_seconds FROM market_metric_hour cannot answer because a blind hour yields no row (source query RollupJob.cs:227-245 reads market_snapshot, MetricHour.cs:36-39 throws on empty) and gap_seconds joins only collector='snapshot' at exchange level (RollupJob.cs:290-294) | The distinguishing case — total blindness — produces no row at all, and depth/candle/WS/process-down blindness contributes zero gap seconds; fails S1,S2,S3,S5,S6. |

**Not mentioned by the prior audit:**

- Every live expected_count value (probe: distinct {120,360}) comes from the pre-fix formula 3600/interval; the current code's least(3600/greatest(interval,60),32767) at RollupJob.cs:277 yields 60 for every configured cadence, so no live row reflects the fix (7eece39) — stored completeness ratios understate by 2-6x, and permanently for any hour outside the rollup's touched window since the upsert only rewrites recent ones.
- gap_seconds' lateral join hardcodes g.collector='snapshot' (RollupJob.cs:292), so depth, candles, funding and discovery blindness never reaches the health block even though the same row carries depth_bid_25bps_avg — live METRICHOUR_DEPTH shows weex 2010/23135 and hyperliquid 247/4071 hours with any depth average, all of them reporting gap_seconds=0, i.e. 'we saw everything'.
- Four of the eight cause values in the collection_gap check constraint (0017:53-61) are unreachable: CauseOf (ExchangeWorker.cs:425-434) emits only rate_limited/timeout/exchange_maintenance/error. A detected Kraken WS sequence gap is a LogWarning and a resync only (KrakenWsFeed.cs:224-227), a WS drop or idle-abort is a LogWarning only (WsConnection.cs:74,164), and nothing writes collector_down — so process downtime, the commonest blindness, leaves no record at all.
- The WP8 columns are write-only: no view, endpoint, doc or query in the repository reads expected_count, snapshot_count or gap_seconds; the sole reader of market_metric_hour selects OI/funding/spread (InstrumentStore.cs:166-175).
- The admin latency panel that presents run health carries the last observed value forward across empty buckets (ExchangeStore.cs:342-351, comment at :343), so a collector that stopped running renders as a flat line at its last latency instead of a hole — LOCF in the exact surface WP8 exists to make honest.
- Retention now spares collector_run and collection_gap (RetentionJob.cs:28-33) but still drops market_snapshot partitions (RetentionJob.cs:80-82), so the health record outlives the observations it describes and a rotated month's completeness becomes unverifiable.
- Per-instrument REST failures produce neither a run row nor a gap row: DepthCollector.cs:44-95 has no per-instrument try/catch (one throw kills the whole pass, logged as a single exchange-level failure) and a null depth at :60 is skipped silently.
- Data rows carry no run linkage at all — ExchangeStore.cs:168 states it outright ('Data is upserted with no run id, so time is the only honest join'), so no observation can be attributed to the collection_run that produced it (S5/S9 for every table in scope).

## J. Rollups (WP9)

All rollup logic is src/CryptoSmithX.MarketData.Hub/Rollups/RollupJob.cs + MetricHour.cs; the only rollup tables in the 17 migrations are market_candle (OHLCV, tf>1) and market_metric_hour (hourly microstructure).
No 1s/10s/1m feature layer, no percentile/OHLC/imbalance/flow/cost column, no known_from and no derived_version exist anywhere in the schema.
The rollup loop itself is hosted unconditionally (ExchangeWorker.cs:65,72-73) and does write a persistent, non-rotated table, so the failures below are of content and shape, not of hosting.

| Item | Collected | Stored correctly | Evidence | Note |
|---|---|---|---|---|
| J1 minute and hour layers store distributions not means (spread median/p90/p95/p99/max, depth median/p10/min, cost-to-execute, OI OHLC, imbalance, flow) | PARTIAL | NO | MetricHour.cs:63-70 emits only OpenInterestLast/FundingRateLast/SpreadBpsAvg/DepthBid25BpsAvg/DepthAsk25BpsAvg/SnapshotCount; RollupJob.cs:265-305 writes them to market_metric_hour.open_interest_last/.funding_rate_last/.spread_bps_avg/.depth_bid_25bps_avg/.depth_ask_25bps_avg/.snapshot_count; DDL 0006_assets_and_metrics.sql:177-188 + 0017_collection_health.sql:86-88 has no percentile/OHLC/imbalance/flow/cost column; grep percentile\|median\|p90\|p99\|quantile\|imbalance\|cvd\|microprice over src+tests returns nothing; flow inputs absent (trades seeded disabled, 0014_collections.sql:189,220) | Only 2 means + 2 last-values exist; every required distribution, OI OHLC, imbalance, cost-to-execute and flow column is absent and there is no minute layer at all; fails S1,S2,S3,S4,S5,S6,S8,S9. |
| J2 ladder 1s->10s->1m->1h with nothing jumping 1m->1h | NO | NO | RollupJob.cs:227-248 builds the hour by reading market_snapshot directly and grouping on date_trunc('hour', s.received_at); market_snapshot holds one row per minute (SnapshotCollector.cs:48-50,132); no 1s/10s/1m rollup table exists in any migration (full create-table list: market_candle, market_metric_hour only) and probe ABSENT confirms market_snapshot_10s, flow_1s, flow_10s, flow_1m, book_feature_1s | Microstructure goes minute-granularity raw straight to 1h in one step and no sub-minute layer exists; the OHLCV cascade 1->5->15->60 (RollupJob.cs:100-101) is a different ladder and does not cover features. |
| J3 buckets addressable by bucket END and known_from >= bucket end | NO | NO | market_metric_hour.hour_time is documented as the hour START (0006_assets_and_metrics.sql:179 'начало часа, UTC'); market_candle is keyed by open_time with no close_time (0001_initial.sql:218-235, probe CANDLE COLUMNS); grep known_from over src/CryptoSmithX.Database/Migrations returns nothing; the only availability-ish stamp is market_candle.updated_at / market_metric_hour.updated_at = now(), rewritten on every recompute (RollupJob.cs:188,198,280,305) | Buckets are start-addressed with no availability time; the closed-window guards (RollupJob.cs:181 and 240-241) give finality, not known_from; fails S1,S2,S5,S6,S9. |
| J4 derived_version everywhere and a rebuild tool | NO | NO | grep -i derived_version\|feature_definition\|rebuild over src and tests returns zero hits (only plans/ and the prior audit mention them); market_candle (0001_initial.sql:218-235) and market_metric_hour (0006_assets_and_metrics.sql:177-188) carry no derived_version column; feature_definition absent per probe ABSENT list | No version column, no feature_definition, no invokable rebuild; the only recompute is the watermark-bounded touched-window pass (RollupJob.cs:27-43,83-89,154-210) with no date-range entry point and no checksums. |

**Not mentioned by the prior audit:**

- Live expected_count values 120/360 (probe HOURHEALTH) are impossible under HEAD's formula least(3600 / greatest(coalesce(ec.interval_s, c.default_interval_s, 10), 60), 32767) at RollupJob.cs:277, whose maximum is 60 — the deployed hub predates commit 7eece39, so every populated hour bucket on the test host carries a health denominator 2-6x too high; the prior audit reported the same 360/360/120 numbers as if they were consistent with the code.
- market_metric_hour.gap_seconds is computed per EXCHANGE, not per instrument: the lateral joins collection_gap only on exchange_code and collector='snapshot' (RollupJob.cs:290-294), and probe GAP shows collection_gap.exchange_instrument_id is NULL on every row — so one instrument going blind is invisible in its hour bucket, and one venue-wide 429 is charged to all of that venue's instruments.
- WP9 requires the WP8 health block on every rollup bucket, but gap_count and max_gap_seconds do not exist (0017_collection_health.sql:86-88 adds only expected_count and gap_seconds), and derived market_candle rows (tf>1) carry no health columns at all beyond bar_count (0001_initial.sql:218-235).
- The permanent hour layer preserves only the 25bps depth band by design (0006_assets_and_metrics.sql:30,190-193); the 10/50bps bands, bid/ask sizes, mark and index all die with the 90-day partition drop in RetentionJob.cs:64-84, so the distributions WP9 asks for can never be backfilled for data already rotated.
- Even a correct hourly distribution would be computed over at most ~60 samples: market_snapshot history is gated to one row per minute (SnapshotCollector.cs:48-50,132,169-172), so 5 of every 6 ten-second observations never reach the rollup input — probe A1 shows 1438-1443 rows/instrument/day against the required ~8640.
- market_metric_hour is upserted whole on every touch (RollupJob.cs:296-305) with updated_at overwritten, so a recomputed hour silently replaces the previous one — no version, no prior row retained, no way to tell which formula produced a stored value (S1 and S6 breach with no audit trail).

## K. Raw archive (WP2)

WP2 has no implementation of any kind: no archive writer, no archive table, no envelope type, no compression dependency, no rebuild CLI. The only verbatim exchange payload that reaches storage is `exchange_instrument.raw_json`, which is overwritten every discovery pass. Separately, an active partition-drop path against `market_snapshot` is hosted and on by default.

| Item | Collected | Stored correctly | Evidence | Note |
|---|---|---|---|---|
| K1 every REST response archived | NO | NO | src/CryptoSmithX.MarketData.Connectors/Kraken/KrakenFuturesClient.cs:73-79 (GetAsync<T> streams straight into ReadFromJsonAsync, body never retained); Weex/WeexFuturesClient.cs:70; Hyperliquid/HyperliquidClient.cs:78; no archive table in any of Migrations/0001-0017; PROBE: /csx-archive, /data/archive, /opt/cryptosmithx/archive ALL ABSENT | Only raw-body persistence anywhere is mutable exchange_instrument.raw_json, overwritten hourly at DiscoveryCollector.cs:123; fails S1,S2,S5,S9. |
| K2 every WS frame archived | NO | NO | src/CryptoSmithX.MarketData.Connectors/Streaming/WsConnection.cs:146-148 (assembled frame text passed to onMessage, then discarded); Kraken/KrakenWsFeed.cs:122-166; Hyperliquid/HyperliquidWsFeed.cs:111-149 — both parse into the in-memory MarketCache only | Frames never leave process memory, and unrecognised frames return before any handler (KrakenWsFeed.cs:145,150; HyperliquidWsFeed.cs:128,133); fails S1,S2,S5,S9. |
| K3 envelope received_at/sent_at/url/channel/status/type/byte length/body verbatim | NO | NO | Repo-wide grep for archive\|ndjson\|zstd\|rebuild over src/**.cs and Migrations/*.sql returns zero hits outside comments; nearest columns are collector_run.transport/http_status added at Migrations/0017_collection_health.sql:24-29; PROBE RUNCOLS: http_status_null=23174 of 23174 | url/channel/sent_at/byte-length/body exist in no table or type, and the one status column that exists is never written (ExchangeWorker.cs:359-371); fails S2,S5,S9. |
| K4 partitioned zstd layout | NO | NO | No zstd/Parquet/compression PackageReference in any of the five csproj files; deploy/provision-host.sh creates no archive directory (mkdir only at :53,:105,:201,:254); hub service declares no volumes in deploy/docker-compose.prod.yml:39-49 or deploy/docker-compose.vm.yml:29-38; PROBE: all archive roots absent | No writer, no compression dependency, and no durable mount for one even if written; fails S1,S2,S5,S9. |
| K5 rebuild tool with checksums | NO | NO | The only CLI in the solution is the one-shot migrator, src/CryptoSmithX.Database/Program.cs:1-28, which takes no subcommands; CryptoSmithX.sln:8-24 lists five projects, none an archive or rebuild tool; grep finds no 'archive_rebuild' source value in Migrations/*.sql or src | T8 is unrunnable — there is no archive to replay and no checksum comparison path; fails S1,S2,S5,S6,S9. |
| K6 no rotation job for any feed except a measured book-frames policy written down with its measurement | NO | NO | src/CryptoSmithX.MarketData.Hub/Retention/RetentionJob.cs:80-82 executes `drop table if exists market_snapshot_YYYY_MM`; hosted unconditionally as a daily loop at Ingestion/ExchangeWorker.cs:74 and 285-314; window read at RetentionJob.cs:45 via DbSettings.cs:229-230 from collection.default_retention_days seeded to 90 for both 'snapshot' and 'depth' at Migrations/0014_collections.sql:184-185 | An enabled-by-default rotation destroys the only observation table; no book frames exist to measure and no measurement is written down; fails S1. |

**Not mentioned by the prior audit:**

- exchange_instrument.raw_json (Migrations/0001_initial.sql:89) is the single place a verbatim venue payload reaches Postgres — and DiscoveryCollector.cs:102,123 upserts it, so every prior discovery body is destroyed hourly. The nearest thing the system has to WP2 is itself mutable and lossy.
- The hub container mounts no volume in either deployed compose file (deploy/docker-compose.prod.yml:39-49; deploy/docker-compose.vm.yml:29-38). K4 therefore fails structurally, not merely because the directory is absent on the host: anything written to /data/archive inside the container would be lost on the next deploy.
- No compression or columnar dependency exists in the solution at all — none of the five csproj files reference zstd, Parquet, or any archive format library. WP2 is not partially scaffolded; nothing has been started.
- The 'depth' collection also carries default_retention_days = 90 (Migrations/0014_collections.sql:185), and depth bands physically live in market_snapshot's own columns (depth_bid_10bps..depth_at, 0001_initial.sql:193-200), so the snapshot partition drop silently takes the depth history with it — one job, two feeds destroyed.
- The drop has not fired yet: PROBE shows partitions only market_snapshot_2026_08/09/10, all inside the 90-day window. The first irreversible loss is prospective (2026_08 becomes droppable around 2026-11-30), so the window to disable RetentionJob before any data is lost is still open. The prior audit does not say this, which makes the finding read as already-realised damage rather than a preventable one.
- None of the provenance fields WP2 depends on exist anywhere in the codebase: repo-wide grep for known_from, collection_run_id, source_seq and archive_rebuild across src/**.cs and Migrations/*.sql returns zero hits. An archive_rebuild source tag could not be written even if the rebuild tool existed.
- WS frames the handlers do not recognise are discarded before any code can observe them (KrakenWsFeed.cs:145,150; HyperliquidWsFeed.cs:128,133 return on unmatched event/channel). A raw archive exists precisely to preserve those frames, so retrofitting a tap at the parser layer would still not satisfy K2 — the hook has to be in WsConnection.cs:146-148.

## L. Point-in-time query layer (WP10)

Nothing of WP10 exists: grep over src+tests for as_of/known_from/event_time/source_seq/collection_run_id/derived_version/valid_from/valid_to returns 0 hits, and the migrations stop at 0017 with no PIT function or view.
The judged deployment (deploy/docker-compose.prod.yml:9-57) runs postgres + migrator + hub + webapp-admin only — MarketData.Api is not deployed there, so the live read surface is the admin WebApp.Admin's ad-hoc SQL.
Every historical read in the tree is "as of now()": age is measured from wall-clock, never from a query time t.

| Item | Collected | Stored correctly | Evidence | Note |
|---|---|---|---|---|
| L1 as_of(instrument,feed,t) filtering event_time<=t AND known_from<=t, returning age | NO | NO | No as_of/known_from/event_time anywhere (grep src+tests = 0 hits); market_snapshot carries only received_at at src/CryptoSmithX.Database/Migrations/0001_initial.sql:177-202 (probe SNAPSHOT COLUMNS); the only 'age' in the tree is now()-received_at over the mutable cache at src/CryptoSmithX.MarketData.Api/Endpoints.cs:134 and src/CryptoSmithX.WebApp.Admin/Data/InstrumentStore.cs:146 | No function, no facade, no knowledge-time column; fails S1 (RetentionJob.cs:78-81 drops partitions), S2, S3, S5, S8, S9. |
| L2 universe_as_of(t) | NO | NO | No universe_as_of; universe is read from the current mutable row at src/CryptoSmithX.MarketData.Api/Endpoints.cs:96-122 and src/CryptoSmithX.WebApp.Admin/Data/InstrumentStore.cs:77; exchange_instrument at 0001_initial.sql:69-92 is overwritten in place by src/CryptoSmithX.MarketData.Hub/Ingestion/DiscoveryCollector.cs:103-121 and UPDATEd to 'delisted' at DiscoveryCollector.cs:157-161; instrument_spec_version and instrument_status_event ABSENT (probe) | Specs and status are a mutable current row, so a past universe cannot be reconstructed; fails S1, S2, S5, S7, S9. |
| L3 book_as_of(instrument,t) | NO | NO | No book_as_of function or facade; book_topn and book_feature_1s ABSENT on test (probe); only band sums exist at 0001_initial.sql:194-200, and the history row copies depth_* out of the mutable cache at src/CryptoSmithX.MarketData.Hub/Ingestion/SnapshotCollector.cs:146-162 | No top-N store, and stored depth is already carried forward at write time; fails S1, S2, S4, S5, S8, S9. |
| L4 candles_closed_before(instrument,t) | NO | NO | No such function or facade; market_candle has no close_time/is_final/source/known_from/price_type at 0001_initial.sql:218-235 (probe CANDLE COLUMNS); non-PIT reads order by open_time at src/CryptoSmithX.MarketData.Api/Endpoints.cs:190-206 and src/CryptoSmithX.WebApp.Admin/Data/InstrumentStore.cs:153-163 | Bars are open-time keyed and upserted (CandleCollector.cs:88-97, RollupJob.cs:184-199), so closure and knowledge time are inexpressible; fails S1, S2, S5, S6, S9. |
| L5 flow_window(instrument,t_from,t_to) with knowledge filter | NO | NO | No flow_window in src; trade, flow_1s, flow_10s, flow_1m, flow_1h all ABSENT on test (probe ABSENT list); trades collection disabled (probe POLICY); no trade parser or writer in src/CryptoSmithX.MarketData.Hub/Ingestion | No trade input, no flow tables and no knowledge-time filter to apply; fails S2, S4, S5, S6, S9. |
| L6 no other read path into historical tables from research/replay | NO | NO | No PIT facade and no research/replay project in the solution (full src/tests listing); the deployed WebApp.Admin reads history directly at src/CryptoSmithX.WebApp.Admin/Data/InstrumentStore.cs:153-163,166-176,180-187,239-251 and src/CryptoSmithX.WebApp.Admin/Data/ExchangeStore.cs:398,423,430; no grant/revoke/role/RLS in any migration | The boundary does not exist in any form — code, view, or database privilege; fails S1, S2, S5, S8, S9. |
| L7 resolution boundary crossing reported or refused | NO | NO | RollupJob picks the source resolution silently at src/CryptoSmithX.MarketData.Hub/Rollups/RollupJob.cs:100-101 and stamps no resolution on the row (probe CANDLE COLUMNS has no source/resolution column); the only validation on a consumer query is tf>0 at src/CryptoSmithX.MarketData.Api/Endpoints.cs:182-185 | Nothing reports or refuses a crossing, and derived bars carry no derived_version; fails S6, S8, S9. |
| L8 *_latest caches unreachable from research/replay | NO | NO | market_snapshot_latest is upserted every ~10s at src/CryptoSmithX.MarketData.Hub/Ingestion/SnapshotCollector.cs:93-128 and read directly by the deployed WebApp.Admin at Data/ExchangeStore.cs:91, Data/DashboardStore.cs:161, Data/InstrumentStore.cs:77,147, Data/AssetStore.cs:31,35,73; the schema documents it as the bot read path at 0001_initial.sql:116-117 | No grants, roles, RLS or facade restrict it, and the design intends consumers to read it; fails S1, S2, S8, S9. |

**Not mentioned by the prior audit:**

- market_snapshot history is not an independent observation: SnapshotCollector.cs:146-162 SELECTs depth_bid_10bps..depth_at out of the mutable market_snapshot_latest cache and inserts them into the history row under the ticker's received_at. Any future as_of over market_snapshot therefore returns forward-filled depth — an LOCF baked into storage at write time (S8), and the reason DEPTHHIST now shows depth in history at all. Section L of the prior audit never mentions this.
- The one table an as_of could read is actively deleted: RetentionJob.cs:78-81 drops market_snapshot month partitions at the 'snapshot' default of 90 days seeded in 0014_collections.sql:184, wired into the deployed hub via Program.cs:32 -> ExchangeWorker.cs:285-303. Probe PARTITIONS confirms only market_snapshot_2026_08/09/10 survive. That is an S1 failure the prior L1 note omits, and it directly contradicts the brief's 'do not delete anything'.
- The schema states the inverse of L8 as design intent, not merely an unenforced gap: 0001_initial.sql:116-117 says market_snapshot_latest is 'what the bots read' and that its received_at is 'asOf' for them. L8 is not a missing guard on a neutral cache; it is a documented consumer contract that must be revoked.
- Grep for as_of, known_from, event_time, exchange_ts, persisted_at, source_seq, collection_run_id, derived_version, valid_from and valid_to across src and tests returns exactly 0 hits. No WP10 item has even a column-level foundation, so none of L1-L8 could be raised to PARTIAL by adding a function alone.
- market_snapshot's observation columns are NOT NULL (0001_initial.sql:181-192), so an unobserved open interest or size is persisted as 0, not NULL — probe S3ZERO: open_interest=0 in 3601 rows and ask_size=0 in 311 rows in 24h. An as_of built on this table would return fabricated zeros with a fresh age rather than nulls, breaking S3 and invariant 1 before any PIT function is written.
- The judged deployment has no market-data read API at all (deploy/docker-compose.prod.yml:9-57 omits the api service), so WP10's 'thin .NET facade' has no host process to live in on the deployed profile — a deployment-shape gap, separate from the missing code.

## M. Replay and parity (WP11)

Repo-wide grep for replay|parity|mismatch|backtest|as_of|slippage|cost.to.execute|taker over all non-obj/bin files returns hits only inside plans/studio-data-rework-brief.md, docs/audit/coverage-2026-09-05.md, and three connector-test doc comments ("replays a fixture"). No harness, no cost model, no parity report, no ADR (docs/adr/ does not exist).
The deployed profile could not run one: deploy/docker-compose.prod.yml:10-58 hosts postgres, migrator, hub, webapp-admin only, and the hub registers one hosted service (src/CryptoSmithX.MarketData.Hub/Program.cs:32 -> ExchangeWorker), whose bodies are discovery/snapshot/depth/candles/funding plus rollup and retention (ExchangeWorker.cs:253-268, 65-66).
I reach the same three NO/NO verdicts as the prior audit; my disagreements are with its evidence and failing-criteria lists, not its verdicts.

| Item | Collected | Stored correctly | Evidence | Note |
|---|---|---|---|---|
| M1 replay harness serves only PIT views | NO | NO | No harness in src/ or tests/ (repo-wide grep, only brief/audit/test-comment hits); hub hosts only ExchangeWorker (src/CryptoSmithX.MarketData.Hub/Program.cs:32; Ingestion/ExchangeWorker.cs:253-268); no PIT function exists — the only migration functions are create_month_partition (src/CryptoSmithX.Database/Migrations/0001_initial.sql:295) and three notify triggers (0015_live_notify.sql:27,38,49), the only view is current-state instrument_v (0006_assets_and_metrics.sql:133); the API serves the mutable cache market_snapshot_latest (src/CryptoSmithX.MarketData.Api/Endpoints.cs:158,169) | Nothing exists to serve, and the tables it would read fail S1 (RetentionJob.cs:56-85 drops market_snapshot partitions; CandleCollector.cs:90 upserts market_candle), S2/S5/S9 (market_snapshot has only received_at, 0001_initial.sql:177-201) and S8 (SnapshotCollector.cs:156-159). |
| M2 cost model from stored book state, taker fees, funding over holding period | NO | NO | No cost/fill/PnL code anywhere (grep slippage\|cost.to.execute\|taker\|fee over src+tests: zero hits outside brief/audit); book state persisted is band sums only, written to the mutable cache by DepthCollector.cs:66-71 and copied into history from that cache by SnapshotCollector.cs:152-159 into market_snapshot.depth_bid_25bps/depth_at; no fee column exists in any of the 17 migrations (only exchange_instrument.contract_multiplier, 0001_initial.sql:76); funding inputs are funding_rate_history(exchange_instrument_id, funding_time, rate) (0006_assets_and_metrics.sql:154-158) plus mutable exchange_instrument.funding_interval_hours (0001_initial.sql:81); probe: book_topn and book_feature_1s ABSENT | All three legs missing: no cost-to-execute ladder, zero persisted fee data, and funding spec is overwritten in place — fails S1,S2,S4,S5,S6,S7,S8,S9. |
| M3 parity report against old-bot export with mismatch classes and unexplained-mismatch metric | NO | NO | No parity report, mismatch taxonomy, metric or importer in the repo (grep parity\|mismatch\|unexplained: only brief and prior-audit hits); docs/ holds only audit/coverage-2026-09-05.md, decisions.md, product-vision.md, recovery-playbook.md — no docs/adr/ and no WP11 mention in any of them; the nearest input, bot_event(utc, type, payload, received_at) (0003_webapp-admin.sql:76-86, written by WebApp.Admin/Api/IngestEndpoints.cs:70-87), is read only for the admin list (WebApp.Admin/Data/BotStore.cs) | Required acceptance artifact absent, and the 45-day Kraken evidence window is ~1440 rows/instrument/day (probe A1) with partitions dropped at 90 days (RetentionJob.cs:56-85, default from 0014_collections.sql:184). |

**Not mentioned by the prior audit:**

- Taker fees have no persisted input at all: grep across all 17 migrations finds no maker/taker/fee column or table (the only economics column is exchange_instrument.contract_multiplier, 0001_initial.sql:76), so M2's fee leg is blocked at the schema level, not just at the code level.
- Funding accrual over a past holding period would use present-day specs: funding_rate_history carries only (exchange_instrument_id, funding_time, rate not null) with no known_from or source (0006_assets_and_metrics.sql:154-158), and the interval/sign spec lives in exchange_instrument.funding_interval_hours (0001_initial.sql:81) which DiscoveryCollector.cs:103-115 overwrites on every pass — brief section 5's mutable-instrument trap, live in the code today.
- market_candle, the only never-rotated series a replay could price fills from, is mutated after the fact: CandleCollector.cs:86-100 does an upsert with do update set, and the table has no close_time, is_final, known_from or price_type (0001_initial.sql:218-235) — so entry/exit prices in any parity run would come from rows rewritten later, with no knowledge-time filter available.
- The parity window is being deleted while the evidence is still at the wrong resolution: RetentionJob.cs:56-85 drops market_snapshot monthly partitions past the 90-day default (0014_collections.sql:184), the probe shows only three partitions (2026_08/09/10) and ~1438-1443 rows/instrument/day against the required ~8640 — a 45-day Kraken replay would see one observation per minute, not per 10 s.
- Nothing anywhere in the repository plans WP11: docs/decisions.md, docs/product-vision.md, docs/recovery-playbook.md and plans/README.md contain no mention of replay, parity, harness or backtest, and docs/adr/ does not exist, so the required ADR and parity-report deliverables have no home in the tree.

## N. Budget governance (WP12)

WP12 (brief:219-222) asks for four things: local weight accounting, a per-connection subscription registry, WS pooling, and 429/disconnect accounting routed into WP8. Migration 0017 added the columns for the first and fourth; no writer fills them. The only budget knowledge in the repo is inline comments with fixed pacing constants, and docs/decisions.md (39 lines) has no rate-limit ADR entry at all, so WP12's "verify limits against live documentation, write findings to the ADR with dates" is unmet outright.

| Item | Collected | Stored correctly | Evidence | Note |
|---|---|---|---|---|
| N1 Hyperliquid weight accounting computed locally | NO | NO | src/CryptoSmithX.MarketData.Connectors/Hyperliquid/HyperliquidClient.cs:74-80 (POST + EnsureSuccessStatusCode, no counter); insert omits the column at src/CryptoSmithX.MarketData.Hub/Ingestion/ExchangeWorker.cs:361-364; column defined src/CryptoSmithX.Database/Migrations/0017_collection_health.sql:28; probe RUNCOLS request_weight_null=23174/23174 → collector_run.request_weight | No weight ledger, window counter or header read exists anywhere; the 0017 column has zero writers. |
| N2 subscription registry per WS connection | NO | NO | KrakenWsFeed.cs:33,43 and HyperliquidWsFeed.cs:36,45 hold a per-feed string[] intent and each build their own WsConnection; subscription acks discarded at KrakenWsFeed.cs:137-145; no registry/pool table in any migration | An unverified desired-symbol array is not a per-connection registry; no pooling, no cap allocation, no table — S5,S9 unreachable. |
| N3 429/disconnect accounting lands in collection_run/collection_gap | PARTIAL | PARTIAL | 429→'rate_limited' at ExchangeWorker.cs:425-434, gap insert at 408-417 (probe GAP: 9 hyperliquid/snapshot/rate_limited rows); disconnects only logged at WsConnection.cs:72-75,164; HyperliquidBookFeed.cs:39-44 and WeexOpenInterestFeed.cs:85-90 swallow failures at LogDebug → collection_gap.cause | Missing: every disconnect/ws_sequence_gap/resync/collector_down cause (0017:56-60 has no writer) and http_status; stored fails S1 (UPDATE close, ExchangeWorker.cs:392-398), S2, S5, S9. |
| N4 remaining REST polling randomized with jitter and per-instrument received_at | PARTIAL | NO | Jitter only between passes, CollectorLoop.cs:158-160; fixed order+constant pace at DepthCollector.cs:21,24-30, HyperliquidBookFeed.cs:24,75-79, WeexOpenInterestFeed.cs:21,121; per-instrument time only Depth.cs:16 (WeexFuturesMarketData.cs:232) and OpenInterestAt (WeexFuturesMarketData.cs:156); batched ticker shares one stamp (HyperliquidMarketData.cs:98,125; WeexFuturesMarketData.cs:108,141; KrakenFuturesMarketData.cs:96,108) | No intra-pass randomization; depth lands only in cache market_snapshot_latest (DepthCollector.cs:64-84) then LOCF-copied (SnapshotCollector.cs:144-162) — fails S1,S2,S5,S8,S9; market_candle has no received_at at all. |

**Not mentioned by the prior audit:**

- Kraken WS discards subscription acknowledgements (KrakenWsFeed.cs:137-145): every 'event' frame returns early after logging only error/alert, so a subscribe that the venue rejected leaves _symbols claiming coverage that does not exist — the in-memory registry is unverified intent, not state.
- HyperliquidBookFeed and WeexOpenInterestFeed are unsupervised background REST cyclers outside CollectorLoop that swallow every per-symbol failure at LogDebug (HyperliquidBookFeed.cs:39-44; WeexOpenInterestFeed.cs:85-90). They spend real budget (800 ms and 150 ms pacing, ~1000 symbols) and produce neither collector_run nor collection_gap rows, so their 429s are invisible to WP8.
- CollectorAttempt.Transport is a hard default of "rest" (CollectorLoop.cs:17) and no call site anywhere passes "ws" (only ExchangeWorker.cs:364,369 consume it), so the transport column added by 0017 is structurally incapable of ever showing WS — not merely unpopulated today. Probe RUNCOLS transport_null=22381/23174 is just the pre-deploy backlog; the 793 populated rows are all 'rest'.
- collector_run.exchange_instrument_id (0017_collection_health.sql:25) has no writer either — ExchangeWorker.cs:361-364 omits it (probe instrument_null=23174/23174), so the per-instrument REST polls WP12 is specifically about (depth, candles) cannot be attributed to an instrument in the run table at all.
- The repo contradicts itself on the Hyperliquid limit: 0017_collection_health.sql:37-39 asserts 1200/min per IP, while HyperliquidBookFeed.cs:19-23 states no documented public limit was found and cites an observed live 429. WP12's ADR requirement is unmet — docs/decisions.md contains no rate-limit entry.
- gap_seconds is derived per-exchange, not per-instrument: RollupJob.cs:285-294 joins collection_gap on exchange_code and collector only (it cannot do better — gap rows carry a NULL instrument), and RollupJob.cs:279 coalesces a missing gap to 0. Combined with the WS/background blindness above, market_metric_hour.gap_seconds stores a measured-looking 0 for hours we were in fact partly blind.
- collector_run itself is now genuinely append-only in code — RetentionJob.cs:28-33 removed the 7-day trim and nothing deletes it — so S1 passes for collector_run even though collection_gap fails it via the UPDATE close.

## O. Acceptance tests T1–T11

CI at HEAD (7eece39, run 33965357828) is green: 107 tests — Connectors 51, Hub 48, WebApp.Admin 8 — and none of them is any of T1–T11.
Local `dotnet test` cannot run here (global.json wants SDK 10.0.400; host has 10.0.300 and 8.0.421), so pass/fail is taken from that CI run, not from the stale prebuilt binaries in the tree.
No test project references a database driver, so no existing test asserts a persisted row, a gap record, a checksum or a schema constraint — the shape every one of T1–T11 requires.

| Item | Collected | Stored correctly | Evidence | Note |
|---|---|---|---|---|
| T1 PIT leak (row with event_time < t < known_from invisible to as_of(t)) | NO | NO | Repo-wide grep of src+tests *.cs/*.sql for as_of\|known_from\|universe_as_of\|book_as_of\|candles_closed_before\|flow_window returns zero code hits; only SQL functions are create_month_partition (0001_initial.sql:295) and the notify triggers (0015_live_notify.sql:27,38,49); market_snapshot has no known_from (0001_initial.sql:128-203; probe SNAPSHOT COLUMNS) | Nothing to test: no as_of and no knowledge-time column exists on any table; fails S2,S5,S9. |
| T2 candle finality (no unclosed bar; candles_closed_before never returns close_time > t) | PARTIAL | NO | Forming-bar drop is asserted at HyperliquidMarketDataTests.cs:137, KrakenFuturesMarketDataTests.cs:59, WeexFuturesMarketDataTests.cs:126, FakeExchangeMarketDataTests.cs:34 (all green, CI run 33965357828); market_candle has no close_time/is_final/source/known_from/price_type (0001_initial.sql:218-235; probe CANDLE COLUMNS) and no candles_closed_before function exists | Only the adapter clause exists; the table-level clause would fail anyway — derived bars are written with bar_count<timeframe and upserted (RollupJob.cs:181-198); fails S1,S2,S6. |
| T3 rollup availability (bucket [a,b) invisible at t < known_from, known_from >= b) | NO | NO | RollupJobTests.cs:18-102 tests only watermark stepping and ladder choice; RollupTests.cs:14-124 tests a pure aggregation helper; no known_from on market_candle (0001_initial.sql:218-235) or market_metric_hour (0017_collection_health.sql:84-86) | Production guards bucket end (RollupJob.cs:181, :241) but records no knowledge time and upserts buckets in place; fails S1,S2,S6,S7. |
| T4 universe as-of (instrument visible only for L <= t < D by knowledge time) | NO | NO | Probe: instrument_spec_version, instrument_status_event, promotion_event ABSENT; exchange_instrument is one mutable row upserted at DiscoveryCollector.cs:103-121 and delisting is an in-place UPDATE at DiscoveryCollector.cs:154-165; no universe_as_of anywhere | Untestable, not merely untested: past specs and listing state are overwritten, so no L/D history exists; fails S1,S7. |
| T5 gap semantics (injected outage → no rows + collection_gap, no zeros) | NO | NO | No test file references collection_gap (grep of tests/ for 'gap' hits only RollupTests.cs:59, RollupJobTests.cs:95, KrakenWsTests.cs:87 and fixture names); the writer at ExchangeWorker.cs:388-417 has no test; probe GAP rows all have exchange_instrument_id NULL | Gap writer ships untested and opens only on whole-collector failure, so per-instrument absence produces no gap; S3 unverified. |
| T6 no LOCF (2h-old OI returned with age=2h; 30s freshness helper rejects it) | NO | NO | No as-of/age/freshness helper over stored data anywhere; SnapshotCollector.cs:146-160 copies depth columns from market_snapshot_latest into every market_snapshot history row; nearest test is KrakenWsTests.cs:22 (in-memory live cache cutoff, returns no age) | The system actively performs LOCF into an append table, so T6 would fail today; fails S8. |
| T7 book resync (sequence gap → gap recorded, resync, no fabricated levels) | PARTIAL | NO | KrakenWsTests.cs:87-102 asserts gap→dirty, depth withheld while dirty, deltas ignored, resync clears (green in CI 33965357828); no collection_gap write for sequence gaps — cause 'ws_sequence_gap' is declared at 0017_collection_health.sql:57 and CauseOf (ExchangeWorker.cs:424-433) can never emit it | In-memory state machine only; the gap record and the absence-of-rows assertion have no producer to test. |
| T8 archive rebuild (drop a parsed partition, rebuild from archive, checksums equal) | NO | NO | No archive writer, rebuild tool or checksum in src (grep archive\|ndjson\|zstd\|parquet\|checksum hits only unrelated comments at ExchangeStore.cs:99 and DashboardStore.cs:217); probe: /csx-archive, /data/archive, /opt/cryptosmithx/archive all ABSENT; RetentionJob.cs:81 drops snapshot partitions outright | Test cannot exist and cannot ever pass: the only copy is dropped with no export; fails S1. |
| T9 cross-exchange staleness (as_of reports per-instrument age; WEEX tail flagged) | NO | NO | No PIT/as-of facade exists (same grep as T1); per-row received_at and depth_at exist (0001_initial.sql:128-155) but nothing computes or surfaces age; WebApp.Admin/Api read market_snapshot directly (Endpoints.cs:197, ExchangeStore.cs:423-430) | No cross-exchange query layer exists to attach the assertion to; fails S8,S9. |
| T10 units (mixing inverse and linear sizes untagged fails at schema level) | NO | NO | No unit-tag column on any size/volume column; units appear only as prose comments (0001_initial.sql:159,161-165); is_inverse was deliberately omitted (0001_initial.sql:29-30) and inverse contracts are excluded at discovery instead (0001_initial.sql:37-38; KrakenFuturesMarketDataTests.cs:22) | No schema-level rejection mechanism exists, so there is nothing for a test to trip; fails S4. |
| T11 replay parity (report generated, mismatch classes enumerated, metric tracked) | NO | NO | No replay harness, cost model, parity report or mismatch taxonomy in the repo (grep parity\|replay matches only fixture-replay prose in adapter tests); no docs/adr directory exists for the section 8 deliverable | WP11 unstarted; the brief's stated acceptance criterion for the whole rework has no artifact. |

**Not mentioned by the prior audit:**

- No test project references a database driver — grep of tests/ for Npgsql\|Testcontainers\|Respawn\|DbConnection returns nothing across all three .csproj files — so every one of T1–T11 (which all assert rows, gap records, checksums or schema constraints) needs test infrastructure that does not exist yet; the gap is structural, not a missing test case.
- RollupTests.cs:91-107 'A_late_minute_changes_the_parent_it_belongs_to' is a green test that pins the in-place mutation of an already-closed rollup bucket — writing T3 would require deleting or inverting this test, not just adding one.
- RollupTests.cs:109-120 asserts that a partial trailing window is emitted as a bar (BarCount 2 of 5), locking in the 'bucket keyed by start, visible before its end' shape that brief section 5 lists as a look-ahead trap.
- Rollup.cs:28 states outright that the class those nine RollupTests cover is not called by the runtime path any more; the actual rollup writer (RollupJob.cs:156-198, the insert...on conflict do update) has no test at all, so T3 has no tested code path to attach to.
- The gap taxonomy value 'ws_sequence_gap' declared at 0017_collection_health.sql:57 is unreachable — CauseOf (ExchangeWorker.cs:424-433) can only emit rate_limited, timeout, exchange_maintenance or error — so T7's gap-record clause lacks a producer, not merely a test.
- CI runs dotnet test on the whole solution for every push (.github/workflows/ci.yml:15) and was green at HEAD, so nothing in the pipeline is currently red on account of the brief; T1–T11 would be the first failing gate, which means their absence is silent rather than reported.
- The prebuilt Release test assemblies checked into the working tree are stale relative to HEAD (38 vs 48 hub cases), so any audit that executes them is measuring a pre-WP8 tree rather than the commit under review.

## P. Retention and legacy

Audited at HEAD 7eece39 — the same commit the prior audit used, so no drift explains any difference. Read-only: no files written, no code changed. All four items land NO/NO, as the prior audit found, but two of its evidence chains are incomplete and one of its conclusions understates a live violation.

| Item | Collected | Stored correctly | Evidence | Note |
|---|---|---|---|---|
| P1 NO retention/deletion job exists for observation tables (YES means confirmed absent) | NO | NO | src/CryptoSmithX.MarketData.Hub/Retention/RetentionJob.cs:54-83 (drop at :80-82) drops market_snapshot_YYYY_MM partitions; registered at Ingestion/ExchangeWorker.cs:55 and looped daily at ExchangeWorker.cs:285-317; enabled by default by collection.default_retention_days=90 for 'snapshot' at Migrations/0014_collections.sql:184, read at RetentionJob.cs:45-49; documented live at src/CryptoSmithX.MarketData.Hub/README.md:88-90; market_snapshot partitions per PARTITIONS probe (2026_08/09/10 only) | A scheduled, default-on deletion job exists against the highest-value observation table; market_snapshot fails S1, and market_candle/market_metric_hour fail S1 by in-place UPDATE (CandleCollector.cs:89-97, RollupJob.cs:190-198, RollupJob.cs:299-302). |
| P2 hot->Parquet export is a verified move before drop | NO | NO | Repo-wide grep for parquet/zstd/ndjson/archive across src, tests and deploy returns only prose (ExchangeStore.cs:99-100, DashboardStore.cs:217) — no writer, no checksum, no rebuild tool; RetentionJob.cs:80-82 issues 'drop table if exists' with no preceding export; deploy/docker-compose.prod.yml:40-52 mounts no archive volume for the hub; probe ARCHIVE paths /csx-archive, /data/archive, /opt/cryptosmithx/archive ALL ABSENT on host | Nothing is exported before the drop; the only pre-drop survivor is the lossy hourly mean market_metric_hour, which the schema itself calls a partial substitute (0006_assets_and_metrics.sql:190-193). Fails S1. |
| P3 legacy rows tagged source=legacy and known_from=persisted_at, old tables read-only | NO | NO | market_snapshot DDL Migrations/0001_initial.sql:177-203 and market_candle 0001_initial.sql:218-235 carry no source, known_from, persisted_at, event_time, exchange_ts, source_seq or collection_run_id; grep for those names across Migrations/*.sql matches only exchange_collection_capability.source (0014_collections.sql:145,167), an unrelated declared/probed/manual tag; the old tables are the current live write target (SnapshotCollector.cs:146-162, CandleCollector.cs:86-97, RollupJob.cs:184-198, RollupJob.cs:265-302) and no revoke/rule/trigger exists in any migration; SNAPSHOT COLUMNS probe confirms the deployed shape | No legacy tagging exists and the old tables are still written and mutated rather than frozen; fails S1, S2, S3 (Ticker.cs:13-27 non-nullable → probe open_interest=0 in 3601 rows), S5, S6, S9 (2 of 9 common columns) — S4 units survive only as prose comments on the sibling latest table. |
| P4 cross-exchange joins never use one shared timestamp | NO | NO | No as_of/PIT/join facade anywhere (repo grep, no SQL functions beyond create_month_partition at 0001_initial.sql:295); src/CryptoSmithX.MarketData.Api/Endpoints.cs:166-176 returns every exchange's rows under one envelope asOf = max(l.received_at) from the mutable cache the schema names as the bots' read path (0001_initial.sql:116-117); market_snapshot rows carry only received_at (0001_initial.sql:177-203) while the depth columns in the same row come from market_snapshot_latest under their own depth_at (SnapshotCollector.cs:144-162); no T9 test exists in tests/CryptoSmithX.MarketData.Hub.Tests/ | No layer guarantees the property, and the one live cross-exchange response collapses three venues onto a single max timestamp; fails S2, S8 (write-time LOCF of depth), S9. |

**Not mentioned by the prior audit:**

- Depth is last-observation-carried-forward at write time, not just in queries: every market_snapshot history row copies the depth bands and depth_at from the mutable market_snapshot_latest cache (SnapshotCollector.cs:144-162), so on WEEX (~468 s depth pass against a 60 s history flush) one book measurement is stored as many repeated rows. The prior audit never mentions depth_at at all, and its DEPTHHIST-style coverage numbers read as depth capture when most of those rows are duplicates.
- market_metric_hour — the only table the 90-day drop is meant to leave behind — averages those duplicates: RollupJob.cs:229-237 selects depth_bid_25bps/depth_ask_25bps but not depth_at, and MetricHour.cs:68-69,74-84 takes a plain unweighted mean, so the permanent hourly depth is duplicate-weighted and carries no record of when the book was actually measured.
- The prior audit never establishes that the deletion path is enabled by default, which the collected criteria require: collection.default_retention_days = 90 for 'snapshot' at Migrations/0014_collections.sql:184 is exactly what lets RetentionJob.cs:45-49 past its null guard. Without that row the job would return 0 and delete nothing.
- The repository states in its own schema what the drop destroys: 0006_assets_and_metrics.sql:190-193 says snapshots die after 90 days and only the 25 bps depth slice is duplicated forever, so the 10 bps and 50 bps bands, bid/ask sizes, mark and index price and turnover survive nowhere. That is the project admitting in DDL that the brief's non-replenishable data is on a 90-day clock.
- Movement in P's direction that section P omits: 0017_collection_health.sql:19-22 and RetentionJob.cs:28-33 removed the 7-day retention that used to trim collector_run, so the health layer is now kept forever. market_snapshot is the only observation table with a deletion path left — a fact that should shape the fix, since P1 is now one config row and one code block away from YES.
- No test covers any of this: tests/CryptoSmithX.MarketData.Hub.Tests/ contains no RetentionJob test, so the drop is unguarded by CI and a change to default_retention_days would silently widen or narrow data loss.

## Summary

| Verdict | Collected | Stored correctly |
|---|---|---|
| YES | 10 | 0 |
| PARTIAL | 34 | 7 |
| NO | 82 | 119 |
| UNKNOWN | 0 | 0 |
| N/A | 3 | 3 |

129 items across sixteen sections. **Not one item reaches `Stored correctly = YES`.**

## Diff vs the prior audit

Every disagreement below was handed to a checker whose instruction was to refute it and to
default to the prior audit when the evidence did not settle the question.

| § | Point | Prior audit | This audit | Checker sided with |
|---|---|---|---|---|
| A | A3, A4, A5, A6, A7, A8, A9, A12, A14 — the field paths | collected=PARTIAL for all nine, on the single ground that 0005_exchange_admin.sql:15-27 le | collected=YES for all nine | not re-checked |
| A | A1 stored-criteria set | fails S1,S2,S3,S4,S5,S8,S9 | same set, but S4 is total, not partial: market_snapshot has zero column comments | not re-checked |
| A | A11 next funding time | NO/NO, evidence: Kraken WS field dropped at KrakenWsDtos.cs:10-27 | NO/NO, same verdict, evidence incomplete | me |
| B | B8 liquidation flag — Kraken | NO / NO, evidenced only by the REST Market History endpoint | NO / NO, but the fact is on the WS trade feed the hub already has open | not re-checked |
| B | B9 exchange sequence — Kraken | NO, citing "continuous trade_id" from the REST history endpoint | NO, but the documented sequence is `seq` on the WS trade feed | not re-checked |
| B | B1–B11 stored notes (failing S-criteria) | "fails S2,S4,S5,S9" per row | S1–S9 are all unmet because no `trade` table exists at all | not re-checked |
| B | B9 exchange sequence — WEEX | NO, "WEEX V3 public trades provide `t` and document ordering checks" | NO, with the caveat that WEEX exposes no sequence field, only an incremental trade id | not re-checked |
| C | C6 — supporting evidence, not the verdict | NO / NO, evidenced as "legacy depth is updated in market_snapshot_latest by DepthCollector | NO / NO, but depth does now reach the persistent partitioned table | not re-checked |
| C | C3 — which S-criteria fail | PARTIAL / NO, "fails S1,S2,S5,S8,S9" | PARTIAL / NO, "fails S2,S3,S5,S8,S9" — S1 holds, S3 is the one that breaks | not re-checked |
| C | C1 — Hyperliquid characterisation | PARTIAL, "HL has full-snapshot WS plus always-on REST" | PARTIAL, same verdict, but HL has no delta channel to validate at all | prior |
| D | D1 spread bp — stored | NO | PARTIAL | prior |
| D | D4 top-of-book sizes — stored | NO | PARTIAL | prior |
| D | D4 top-of-book sizes — failing-criteria list | fails S1,S2,S4,S5,S6,S8,S9 | fails S1,S2,S3,S4,S5,S9 | unclear |
| D | D5 depth bands — stored | NO | PARTIAL | prior |
| D | D14 cadences — stored | NO | PARTIAL | prior |
| D | D5 depth bands — S-criteria list | fails S1,S2,S5,S6,S8,S9 | fails S1,S2,S5,S8,S9 | prior |
| E | E1 trade count — prior note "default gate missing" | PARTIAL/NO, "HL candle counts at coarse cadence exist, with default gate missing" | PARTIAL/NO, but candles are enabled by default and the gate is present | prior |
| E | E11 cadences — prior note "default gate missing" | PARTIAL/NO, "default gate missing" | PARTIAL/NO; the 1m and 1h paths are hosted and enabled, and the rollup loop is uncondition | prior |
| E | E1 storage criteria list | fails S1,S2,S5,S6,S9 | also fails S3 | prior |
| F | F2 Hyperliquid at least hourly | PARTIAL collected, reason "default real-exchange status is planned" | PARTIAL collected, reason: 3 h backfill floor | not re-checked |
| F | F5 unclosed bars never canonical | PARTIAL collected ("defaults are planned") | YES collected | not re-checked |
| F | F1/F2/F4/F5 failing S-criteria list | fails S1,S2,S5,S9 | fails S1,S2,S4,S5,S7,S9 | not re-checked |
| G | G1 realized funding history with known_from | PARTIAL / NO, failing S2,S5,S9 | PARTIAL / NO, failing S2,S3,S5,S9 | me |
| G | G2 funding_schedule SCD2 — evidence for the interval fact | "Mutable interval at DiscoveryCollector.cs:93-124; WEEX substitutes 8 h when missing" | Same verdict (PARTIAL / NO) but the interval is not an observation at all on two of three  | me |
| H | S-criteria applied to exchange_instrument (H1-H5 notes) | Different failing sets per row for one table: H1 S1,S7,S9; H2 S1,S2,S3,S7,S9; H3 S1,S2,S5, | Uniform: S1,S2,S3,S4,S5,S7,S8,S9 all fail; S6 is N/A | prior |
| H | H1 — what actually breaks identity | PARTIAL/NO because "native-id identity does not" exist | PARTIAL/NO, but the operative defects are that first_seen is not in the key and the row is | not re-checked |
| H | H5 — how far the cadence can slip | "Starts can exceed 60 min" from 0.9-1.1x post-run jitter (CollectorLoop.cs:85-160) | Same PARTIAL, but the bound is ~5 h, not ~66 min | not re-checked |
| I | I3 stored | PARTIAL | NO | not re-checked |
| I | I1 — prior's claim that 'finished time' is missing | finished time missing | finish time is present and populated | me |
| I | I5 — prior's cited candidate query | NO, with 'SELECT expected_count,snapshot_count,gap_seconds FROM market_metric_hour' offere | NO, and that query is not even a candidate | me |
| J | J1 minute and hour layers store distributions not means | PARTIAL / NO, fails S1,S2,S5,S6,S9 | PARTIAL / NO, fails S1,S2,S3,S4,S5,S6,S8,S9 | me |
| J | J2 ladder 1s->10s->1m->1h with nothing jumping 1m->1h | NO / NO, evidence: 'Candle source selection is minute TFs at RollupJob.cs:91-101; config 5 | NO / NO, but on different evidence | me |
| K | K1 every REST response archived | NO / NO — evidence: "REST clients deserialize responses directly, e.g. KrakenFuturesClient | NO / NO — same verdict, but the cited evidence is over-broad and one raw path is missed | prior |
| K | K6 no rotation job except a measured book-frames policy | NO / NO — "An unverified observation-deletion path exists" | NO / NO — same verdict; the prior understates it as merely existing | me |
| L | L1 / L8 — evidence for the live latest-cache read path | NO / NO, citing src/CryptoSmithX.MarketData.Api/Endpoints.cs:124-176 as the reader of mark | NO / NO, but the cited reader is not deployed on the judged host | prior |
| L | L6 — which code is the offending historical read path | NO / NO, citing 'direct historical reads occur in RollupJob.cs:156-182,227-245' | NO / NO, but RollupJob is the hub's own writer, not a research/replay consumer | me |
| L | L4 — failing S-criteria list | NO / NO, note lists S1,S2,S5,S8,S9 | NO / NO, note must also list S6 | prior |
| M | M2 failing-criteria list | NO / NO, note "fails S2,S4,S5,S6,S9" | NO / NO, fails S1,S2,S4,S5,S6,S7,S8,S9 | me |
| M | M1 characterisation of the data path | NO / NO, note "S2/S8/S9 guarantees are unavailable" | NO / NO, but S8 is actively violated, not merely unavailable | not re-checked |
| M | M3 available parity input | NO / NO, evidence "repository search finds no parity report, mismatch taxonomy, or unexpla | NO / NO, but an unused bot decision/trade store already exists | not re-checked |
| N | N2 subscription registry per WS connection | collected=PARTIAL, stored=N/A | collected=NO, stored=NO | prior |
| N | N3 429/disconnect accounting — scope of what persists | "REST loop 429s persist" | Only CollectorLoop-supervised REST 429s persist; two live REST budget consumers persist no | me |
| O | Test-suite execution status at HEAD | dotnet test could not build; prebuilt Release assemblies run with VSTest gave Hub 38/38, C | HEAD's suite is 107 tests (Connectors 51, Hub 48, WebApp.Admin 8), all green in GitHub Actions r | not re-checked |
| O | T2 candle finality — does a test exist | NO (adjacent parser coverage is not the required test) | PARTIAL — the forming-bar clause is asserted for every adapter | not re-checked |
| O | T7 book resync — does a test exist | NO, while citing KrakenWsTests.cs:86-103 as adjacent coverage | PARTIAL — the detect/dirty/withhold/resync half is tested and green | not re-checked |
| O | T3 rollup availability — evidence quality | NO, citing RollupJobTests.cs:13-102 as covering window/source mechanics | NO, but the stronger point is that an existing green test asserts the forbidden behaviour | not re-checked |
| P | P4 cross-exchange joins never use one shared timestamp | NO / NO — "Required cross-exchange join behavior cannot be demonstrated" | NO / NO — a live violation is demonstrable, not merely absent | prior |
| P | P3 legacy source/known-time tags and read-only old tables | NO / NO — "fails S1,S2,S5,S9" | NO / NO — also fails S3, S6 | prior |

**Score: the checker sided with the prior audit 16 times and with this audit 11 times (1 unclear, 20 not re-checked).** The prior audit is substantially sound; most disagreements concern note wording and which S-criteria to list, not the verdicts themselves.

