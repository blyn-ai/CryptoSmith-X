name: cryptosmithx-marketdata

services:
  postgres:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-marketdata}
      POSTGRES_USER: ${POSTGRES_USER:-marketdata}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-marketdata}
    volumes:
      - marketdata-pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-marketdata} -d ${POSTGRES_DB:-marketdata}"]
      interval: 5s
      timeout: 3s
      retries: 20

  # One-shot: applies the schema, then exits. Hub and api wait for it to complete successfully.
  database-migrator:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
      target: migrator
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      ConnectionStrings__Database: ${DATABASE_CONNECTION_STRING:-Host=postgres;Port=5432;Database=marketdata;Username=marketdata;Password=marketdata}
    restart: "no"

  hub:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
      target: hub
    depends_on:
      database-migrator:
        condition: service_completed_successfully
    environment:
      ConnectionStrings__Database: ${DATABASE_CONNECTION_STRING:-Host=postgres;Port=5432;Database=marketdata;Username=marketdata;Password=marketdata}
    restart: unless-stopped

  api:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
      target: api
    depends_on:
      database-migrator:
        condition: service_completed_successfully
    environment:
      ConnectionStrings__Database: ${DATABASE_CONNECTION_STRING:-Host=postgres;Port=5432;Database=marketdata;Username=marketdata;Password=marketdata}
    ports:
      - "8080:8080"
    restart: unless-stopped

  webapp:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
      target: webapp
    depends_on:
      database-migrator:
        condition: service_completed_successfully
    environment:
      ConnectionStrings__Database: ${DATABASE_CONNECTION_STRING:-Host=postgres;Port=5432;Database=marketdata;Username=marketdata;Password=marketdata}
      # Dev users ship in appsettings.Development.json; run in Development so they load.
      ASPNETCORE_ENVIRONMENT: ${WEBAPP_ENVIRONMENT:-Development}
      # DataProtection keys must survive a restart, or every redeploy logs everyone out.
      DataProtection__KeysDirectory: /keys
    volumes:
      - webapp-keys:/keys
    ports:
      - "8081:8080"
    restart: unless-stopped

volumes:
  marketdata-pgdata:
  webapp-keys:
